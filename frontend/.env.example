# Cấu hình môi trường Frontend — FE_FOUNDATION_SPEC.md Mục 22.
# Copy file này thành .env (hoặc .env.development) và điền giá trị local.
# KHÔNG đặt secret ở đây — mọi biến VITE_* bị bundle public vào code client.

VITE_API_BASE_URL=http://localhost:3000/api
