# DEV-071 — Cập nhật `04_DATABASE_ANALYSIS.md`/`02_ARCHITECTURE.md`: bổ sung 10 model còn thiếu

**Ngày:** 2026-09-21
**Trạng thái:** DONE
**Phạm vi:** Cập nhật (KHÔNG viết file mới) `docs/04_DATABASE_ANALYSIS.md` + `docs/02_ARCHITECTURE.md` +
`docs/00_PROJECT_MEMORY.md` — bổ sung model thiếu, sửa số liệu "21 model" → số thật, giữ ĐÚNG format/evidence
style (CONFIRMED/INFERRED/UNKNOWN) đang dùng trong từng file. Task tài liệu thuần, KHÔNG đụng code.

## 1. Sửa số liệu — 10 model thiếu, KHÔNG PHẢI 7

User yêu cầu ban đầu liệt kê **7 model** thiếu: Vendor, Contract, ConsumableCategory, ConsumableItem,
ConsumableRequest, ConsumableTransaction, TwoFactorOtp. Đếm lại trực tiếp `backend/src/models/` (32 file
`.ts`, trừ `notifications/notification.types.ts` — file type definition thuần, KHÔNG export Mongoose model)
= **31 model thật**, phát hiện thêm **3 model khác** cũng không có trong tài liệu:
`AssetMaintenancePlan` (Roadmap B2), `DocumentPdfExport` (Roadmap B5/`DEV-064`),
`DocumentVersion` (Roadmap A4). Đã báo lại với user trước khi làm (CLAUDE.md §3/§29 — source code là
nguồn sự thật, không âm thầm theo số liệu sai của yêu cầu gốc) và bổ sung ĐỦ 10 model để tài liệu thực sự
khớp source, đúng mục tiêu thật của task (không phải chỉ khớp đúng con số "7" ban đầu).

**Model count**: 21 (gốc) + 10 (mới) = **31**.

## 2. `docs/04_DATABASE_ANALYSIS.md` — thay đổi

- Sửa "21 model" → "31 model" ở 3 chỗ (Mục 1 Database Overview, Mục 3 Connection Architecture, header Mục 4).
- Thêm cảnh báo `[CẬP NHẬT DEV-071]` ở đầu file + đầu Mục 4, giải thích rõ 21 model gốc GIỮ NGUYÊN số thứ
  tự 4.1→4.21 (không renumber — tránh phá vỡ hàng chục tham chiếu "mục 4.X" rải rác ở Mục 6/9/10/12/14 của
  chính file này), 10 model mới thêm ở **4.22→4.31**.
- Viết đủ 10 mục mới theo ĐÚNG format bảng field + index + CONFIRMED/INFERRED/UNKNOWN của phần gốc, đọc
  trực tiếp source từng model (`backend/src/models/vendors/`, `inventory/`, `auth/twoFactorOtp.model.ts`,
  `assets/assetMaintenancePlan.model.ts`, `documents/documentPdfExport.model.ts`/`documentVersion.model.ts`):
  - Mỗi mục có QUAN HỆ (ref ObjectId) tới các model đã có trong file — đúng yêu cầu (VD `Contract → Vendor`
    + `Contract.assets[] → Asset`, `ConsumableRequest.items[].consumableItem → ConsumableItem`, `ConsumableItem →
    ConsumableCategory`/`Department`, `DocumentPdfExport`/`DocumentVersion → Document`).
  - Phát hiện đáng chú ý khi đọc source (không phải suy đoán):
    - `ConsumableCategory` **mirror** `AssetCategory` (comment nguồn tự xác nhận) nhưng CÓ index trên
      `parentCategory` — điều mà `AssetCategory` (mục 4.14 gốc) KHÔNG có (đã ghi UNKNOWN ở đó).
    - `ConsumableItem` có **unique compound `{department, name}`** — pattern "unique theo tenant-like
      scoping" chưa từng xuất hiện ở model nào khác trong hệ thống.
    - `ConsumableRequest.items` có **custom Mongoose `validate:` function** — mâu thuẫn trực tiếp với
      claim cũ ở Mục 11 gốc ("KHÔNG có custom validator function nào") — đã sửa cả 2 chỗ.
    - `DocumentVersion.meta` dùng `Schema.Types.Mixed` — nâng tổng số field Mixed từ 2 lên 3 (đã sửa Mục 5).
    - `Contract`/`ConsumableItem` có field `expiryAlertSentAt`/`lowStockAlertSentAt` — grep xác nhận có
      cron riêng tương ứng (`shared/cron/contractAlerts.cron.ts`, `consumableAlerts.cron.ts`), CHƯA đọc
      nội dung chi tiết (ghi UNKNOWN, không suy đoán logic cụ thể).
  - **Tiện thể sửa 1 lỗi số liệu CŨ không liên quan trực tiếp task** (phát hiện khi thêm `ConsumableCategory.
    parentCategory` làm quan hệ tự tham chiếu thứ 3): Mục 5 gốc ghi "3 quan hệ tự tham chiếu" nhưng chỉ liệt
    kê 2 — nay liệt kê đủ 3, số đếm khớp thật (đúng CLAUDE.md §3, không để tài liệu tự mâu thuẫn).
- Cập nhật Mục 6 (sơ đồ quan hệ ASCII + bảng phân loại 6.1): thêm block quan hệ cho 10 model mới.
- Cập nhật Mục 10 (bảng index tổng hợp): thêm 10 dòng mới + sửa "Tổng kết OBSERVED" (6/21→"7/31" đúng, giữ
  nguyên tử số 7 vì cả 10 model mới đều CÓ index, chỉ mẫu số đổi).
- Cập nhật Mục 15 (Unknowns): đánh dấu RESOLVED 1 unknown cũ (custom validator), thêm 3 unknown mới phát
  sinh từ model mới (thuật toán `DocumentPdfExport.signature`, giới hạn `TwoFactorOtp.attempts` ở tầng
  nào, nội dung 2 cron mới).
- **KHÔNG đụng** Mục 7 (Data Access Flow), Mục 8 (CRUD), Mục 9 (Query Analysis), Mục 12 (Data Integrity),
  Mục 13 (Data Flows), Mục 14 (Risk table) — ngoài phạm vi "bổ sung model thiếu", không tự audit lại risk/
  query pattern của 10 model mới (đúng CLAUDE.md §26 — không blind-refactor/mở rộng scope task tài liệu).

## 3. `docs/02_ARCHITECTURE.md` — thay đổi

Kiểm tra domain Vendors/Inventory/2FA theo đúng yêu cầu — **đều THIẾU hoàn toàn** (0 kết quả grep
`Vendor|Contract|Consumable|TwoFactor|2FA` trước khi sửa):

- Mục 3.2 (bảng route mount): đối chiếu trực tiếp `app.ts` — thiếu 4 prefix
  (`/api/assets/maintenance-plans`, `/api/inventory`, `/api/vendors`, `/api/contracts`), đã bổ sung.
  Không thấy route riêng cho 2FA/PDF export/document version — ghi INFERRED là sub-route lồng trong
  `/api/auths`/`/api/documents` sẵn có, CHƯA xác nhận trực tiếp (để dành phase sau).
- Mục 4 (bảng model theo domain): sửa "21 Mongoose model" → "31", thêm domain MỚI **Vendors**/**Inventory**,
  thêm model mới vào domain **Auth**/**Documents**/**Assets** đã có sẵn.
- Mục 7 (Module Dependencies): thêm 2 dòng cron mới phát hiện (`contractAlerts`/`consumableAlerts` →
  Vendors/Inventory + Notifications), cùng pattern với `assetAlerts`/`medicalDeviceAlerts` đã có — chỉ xác
  nhận file cron TỒN TẠI (grep), không đọc nội dung chi tiết logic bên trong (ngoài phạm vi task).

## 4. `docs/00_PROJECT_MEMORY.md` — thay đổi

2 chỗ nhắc "21 model"/"7/21 model" (Mục tổng quan backend + Mục tóm tắt Phase 04) — sửa thành 31/7-31,
KHÔNG re-verify các số liệu khác trong cùng dòng (route file/dòng/endpoint count) — ghi rõ có thể cũng lỗi
thời nhưng ngoài phạm vi task này (chỉ sửa model count theo đúng yêu cầu).

`docs/frontend/FRONTEND_MEMORY.md` — grep xác nhận KHÔNG có entry nào nhắc "21 model", không cần sửa.

## 5. Phát hiện KHÔNG sửa — 8 file khác cũng nhắc "21 model" (ngoài scope, chỉ báo lại)

Grep toàn `docs/` phát hiện thêm 8 file khác cũng còn ghi "21 model": `15_ARCHITECTURE_REVIEW.md`,
`14_ANALYSIS_AUDIT.md`, `module-reviews/16_DATABASE_CROSS_DOMAIN_REVIEW.md`,
`module-reviews/01_AUTH_CODE_REVIEW.md`, `12_ISSUES_AND_RISKS.md`, `13_FINAL_PROJECT_REPORT.md`,
`11_TECHNICAL_DEBT.md`, `10_PERFORMANCE_ANALYSIS.md`, `01_PROJECT_OVERVIEW.md`. **KHÔNG sửa** trong task
này — user chỉ chỉ định `04_DATABASE_ANALYSIS.md`/`02_ARCHITECTURE.md`/`FRONTEND_MEMORY hoặc
PROJECT_MEMORY`, mở rộng sang 8 file review/audit khác là ngoài phạm vi được giao (CLAUDE.md §9 — không tự
ý mở rộng scope). Ghi nhận lại rõ ràng để user quyết định có cần 1 task riêng dọn tiếp hay không.

## 6. Verify

Task tài liệu thuần — không có thay đổi code, không cần build/test/lint. Đã tự đọc lại toàn bộ đoạn mới
chèn vào `04_DATABASE_ANALYSIS.md` (Mục 4.22→4.31, Mục 5, Mục 6, Mục 10, Mục 15) để xác nhận không phá vỡ
định dạng Markdown (bảng, code block) và không còn "21 model" nào bị bỏ sót dưới dạng claim sống (chỉ còn
trong các câu giải thích lịch sử "trước đó ghi X" — đã kiểm bằng grep lại).

## 7. Việc CẦN LÀM THÊM

- ~~8 file khác ở Mục 5 vẫn còn "21 model"~~ — **ĐÃ LÀM, xem Mục 8** (user yêu cầu tiếp 2026-09-21).
- 3 UNKNOWN mới ghi ở Mục 15 (`04_DATABASE_ANALYSIS.md`) — cần đọc thêm `documentPdfExport.service.ts`,
  `auths.service.ts` (phần OTP), `contractAlerts.cron.ts`/`consumableAlerts.cron.ts` nếu muốn resolve.

## 8. Cập nhật 2026-09-21 — Sửa 8 file (9 file thật, tính cả 2 file trong `module-reviews/`) còn "21 model"

User yêu cầu tiếp: "bạn làm 8 file khác như bạn đề xuất nhé cập nhật và chỉnh lại". Grep lại xác nhận đúng
9 vị trí file (8 tên gốc + `module-reviews/` chứa 2 file riêng biệt): `01_PROJECT_OVERVIEW.md`,
`10_PERFORMANCE_ANALYSIS.md`, `12_ISSUES_AND_RISKS.md`, `13_FINAL_PROJECT_REPORT.md` (7 chỗ),
`11_TECHNICAL_DEBT.md` (2 chỗ), `15_ARCHITECTURE_REVIEW.md` (2 chỗ), `14_ANALYSIS_AUDIT.md`,
`module-reviews/01_AUTH_CODE_REVIEW.md` (2 chỗ), `module-reviews/16_DATABASE_CROSS_DOMAIN_REVIEW.md`.

**Cách sửa — KHÔNG rewrite lịch sử, chỉ annotate**: các file này là báo cáo/snapshot tại thời điểm Phase
01/04/10/11/12/13/14/15 (đã COMPLETED theo CLAUDE.md §6) — số "21 model" ĐÚNG tại thời điểm viết. Thay vì
âm thầm sửa "21"→"31" (làm sai lệch record lịch sử), mỗi vị trí được giữ nguyên số gốc + thêm chú thích
`[CẬP NHẬT DEV-071, 2026-09-21]` trỏ về `docs/04_DATABASE_ANALYSIS.md` (nguồn sự thật hiện tại) — đúng
CLAUDE.md §3 bước 4 ("Đánh dấu documentation là OUTDATED nếu cần") + §29 (documentation sync, áp dụng cả
cho tài liệu lịch sử, không chỉ tài liệu "sống").

- `01_PROJECT_OVERVIEW.md` §8: đây là danh sách FILE thật (không phải chỉ con số) — bổ sung trực tiếp 10
  đường dẫn model mới vào danh sách, thay vì chỉ ghi chú (khác cách xử lý các file report khác).
- Các dòng "7/21 model không có index" (`10_PERFORMANCE_ANALYSIS.md`, `11_TECHNICAL_DEBT.md` x2,
  `12_ISSUES_AND_RISKS.md`, `13_FINAL_PROJECT_REPORT.md`, `14_ANALYSIS_AUDIT.md`,
  `module-reviews/01_AUTH_CODE_REVIEW.md` x2): thêm chú thích tỷ lệ thật nay là **7/31 (≈23%)**, giảm từ
  7/21 (≈33%) — đúng theo phát hiện đã xác nhận ở `04_DATABASE_ANALYSIS.md` §10 (cả 10 model mới đều CÓ
  index, danh sách 7 model thiếu index KHÔNG đổi).
- `15_ARCHITECTURE_REVIEW.md` (data-access layer ratio "20/21"): CHỈ sửa số tổng, **KHÔNG** tự suy ra tỷ lệ
  mới cho 10 model mới (chưa verify domain Vendors/Inventory có tầng `.query.ts` tách riêng hay không) — ghi
  rõ CHƯA re-verify, tránh tạo claim không có evidence (CLAUDE.md §19/§34).
- `module-reviews/16_DATABASE_CROSS_DOMAIN_REVIEW.md`: chỉ là danh sách "nguồn đã đọc" tại thời điểm review
  đó — thêm chú thích ngắn, không mở rộng nội dung review.
- **KHÔNG sửa** `01_PROJECT_OVERVIEW.md` dòng "Frontend: Không tồn tại" (đã lỗi thời rõ ràng vì `frontend/`
  hiện có) — phát hiện tình cờ khi đọc file nhưng ngoài phạm vi task này (chỉ về model count), không tự ý
  mở rộng scope (CLAUDE.md §9/§26).
- Task tài liệu thuần — không build/test. Verify bằng grep lại toàn `docs/` xác nhận không còn "21 model"
  nào là claim sống chưa được chú thích (chỉ còn trong chính `SESSION_HANDOFF.md`/`DEV-071.md` — narrative
  mô tả quá trình sửa, không phải claim kỹ thuật).
