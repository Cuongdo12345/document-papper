# DEV-023 — Dọn Dead Code Tích Luỹ + Hợp Nhất Duplicate Config (P3)

> Ngày: 2026-09-02. Status: **DONE**. `ARCH-02/03/08/11/13/15/34`, `ARCH-14`, `ARCH-26`, `ARCH-35` (REF-018/REF-020).

## Objective

Dọn dead code tích luỹ qua nhiều đợt phát triển (đã ghi nhận CONFIRMED, xác nhận lại KHÔNG đổi qua nhiều phase/module review) + hợp nhất 2 nhóm duplicate config (rate-limiter, Multer).

## Verify lại trên source hiện tại (trước khi xoá bất kỳ dòng nào)

Nguyên tắc: **chỉ xoá khi xác nhận LẠI bằng grep/đọc trực tiếp — không tin mù quáng danh sách cũ từ doc lịch sử.**

| Mục tiêu | Xác minh | Kết quả |
|---|---|---|
| `shared/errors/errorHandler.ts` (file `@deprecated`) | grep toàn `src/` tìm import trỏ tới file | **0 tham chiếu** — an toàn xoá |
| `config/database/mongo.logger.ts` (`registerMongoLogger` không được gọi) | grep toàn `src/` | **0 tham chiếu** — an toàn xoá (đã ghi nhận dead ở DEV-019) |
| `services/upload/upload.validator.ts` (`validateFiles`) | grep toàn `src/` | **0 tham chiếu** — an toàn xoá (đã ghi nhận dead ở DEV-017) |
| `shared/constants/permission.descriptors.ts` | grep toàn `src/` + `scripts/seed-rbac.ts` (không git-track nhưng có trên đĩa) | **0 tham chiếu thật** (chỉ tự nhắc tên trong message lỗi của chính nó) — an toàn xoá |
| `documents.validator.ts:validateStatusTransition/validateStatusPermission` (ARCH-34, UNKNOWN consumer) | grep toàn `src/` | **0 caller** — an toàn xoá. Kéo theo `shared/constants/workflow-docs.ts` (chỉ 2 hàm này dùng) mồ côi hoàn toàn → xoá luôn |
| `workflow.service.ts` ~500 dòng cuối (ARCH-08) | đếm dòng KHÔNG phải comment/blank trong vùng nghi ngờ | **0 dòng code thật** (dòng 626→1130/1130, đúng ranh giới) |
| `assetAssignment.service.ts` ~280 dòng cuối (ARCH-11) | tương tự | **0 dòng code thật** (dòng 352→631/631) |
| `excel.service.ts` ~207 dòng giữa file (ARCH-15/RV08-07) | tương tự, xác nhận có code THẬT tiếp tục SAU khối chết (`listImportHistory`/`syncDepartmentFromExcel`) | **0 dòng code thật** trong đúng vùng 672-878, code thật tiếp tục ngay sau — xoá ĐÚNG vùng, không đụng 2 hàm thật phía sau |
| `middlewares/loadDocument.middleware.ts` (REF-018 liệt kê cùng nhóm, có điều kiện "chỉ xoá nếu Hướng B") | Đối chiếu quyết định DEV-009 (Phương án A — Activate ABAC) | **KHÔNG XOÁ** — file này CHÍNH LÀ middleware DEV-009A (đang PAUSED) cần dùng để wire ABAC domain Document. Xoá sẽ phá hỏng kế hoạch đã PLANNED |
| `rolePermission.map.ts`/`ROLE_PERMISSIONS` (REF-018 liệt kê là "RBAC dead config") | Đối chiếu lại evidence DEV-021 (SEC-09) | **KHÔNG XOÁ** — `scripts/seed-rbac.ts` (không git-track, có trên đĩa) đã xác nhận CÓ dùng `ROLE_PERMISSIONS` — finding gốc OUTDATED, chỉ `permission.descriptors.ts` (file khác) mới thật sự dead |
| `config/database/database.ts` ~55 dòng comment (RV00-06) | đọc lại toàn file (72 dòng) | **OUTDATED/không còn khớp** — file hiện tại chỉ có 2 đoạn docstring mô tả (không phải code chết dạng câu lệnh comment-out), tổng ~20 dòng, không phải "code cũ bị comment nguyên khối" như mô tả gốc. KHÔNG sửa (cosmetic, giá trị thấp, không phải dead code thật) |
| ARCH-02 (Excel đọc thẳng `Department` model) / ARCH-03 (middleware ghi thẳng `UserAudit`) | Đọc lại 2 finding — không có mục "Recommendation" nào, đánh giá INFO/LOW-MEDIUM, tự nhận là "quan sát kiến trúc, không phải bug" | **KHÔNG SỬA** — không phải dead code, không có khuyến nghị hành động cụ thể, sửa sẽ là refactor kiến trúc ngoài phạm vi "dọn dead code" |
| ARCH-35 (2 cấu hình Multer riêng biệt) | Đọc lại finding — tự kết luận "Chấp nhận được ở quy mô hiện tại" | **KHÔNG SỬA** — LOW, finding gốc không yêu cầu hành động, chỉ ghi nhận tham khảo cho tương lai (nếu có loại upload thứ 3) |
| ARCH-26 (2 rate-limiter trùng cấu hình) | Đọc `app.ts` + `authRateLimiter.middleware.ts` + `auth.routes.ts` | **CONFIRMED** — `authLimiter` (app.ts, mount rộng `/api/auths/*`) và `authRateLimiter` (route-level, chỉ 3/5 route) là 2 bộ đếm độc lập, cùng cấu hình 20 req/15p | SỬA — hợp nhất |

## Changes

### Xoá hẳn 5 file (100% dead, xác nhận 0 tham chiếu)

```
backend/src/shared/errors/errorHandler.ts
backend/src/config/database/mongo.logger.ts
backend/src/services/upload/upload.validator.ts
backend/src/shared/constants/permission.descriptors.ts
backend/src/shared/constants/workflow-docs.ts
```

### Xoá code chết bên trong file đang hoạt động (giữ nguyên code thật)

- `services/documents/workflow.service.ts` — xoá 506 dòng cuối (626→1130), giữ nguyên toàn bộ 624 dòng code thật.
- `services/assets/assetDevice/assetAssignment.service.ts` — xoá 281 dòng cuối (352→631, TRƯỚC KHI có 33 dòng mới thêm ở DEV-022), giữ nguyên toàn bộ code thật (kể cả `runAssignmentTransaction` mới thêm ở DEV-022).
- `services/excel/excel.service.ts` — xoá đúng 207 dòng khối giữa file (672→878), giữ nguyên `importDocumentsExcel` (thật, phía trước) và `listImportHistory`/`syncDepartmentFromExcel` (thật, phía sau).
- `services/documents/documents.validator.ts` — xoá `validateStatusTransition`/`validateStatusPermission` (0 caller, đóng đúng TODO đã tự ghi trong code "xác nhận không còn consumer nào rồi xoá hẳn") + import `workflow-docs.ts` không còn cần.

### Hợp nhất rate-limiter (ARCH-26)

- `app.ts` — xoá `authLimiter` (mount rộng `/api/auths/*`) + import `express-rate-limit` không còn dùng.
- `routes/auth/auth.routes.ts` — gắn tường minh `authRateLimiter` vào `/reset-password` (trước đây route này CHỈ được bảo vệ NGẦM qua `authLimiter` chung vừa gỡ — gắn thêm để không mất bảo vệ). `/forgot-password` CHỦ Ý không gắn (đã có giới hạn riêng ở tầng service, 2 mục đích khác nhau, đúng thiết kế ghi trong comment gốc).

### `.env.example` — đồng bộ theo việc xoá `mongo.logger.ts`

Xoá 2 dòng `MONGO_DEBUG=`/`MONGO_SLOW_MS=` (không còn bất kỳ code nào đọc 2 biến này sau khi xoá file) — thay bằng 1 đoạn giải thích lý do.

## Affected Files

```
XOÁ:
backend/src/shared/errors/errorHandler.ts
backend/src/config/database/mongo.logger.ts
backend/src/services/upload/upload.validator.ts
backend/src/shared/constants/permission.descriptors.ts
backend/src/shared/constants/workflow-docs.ts

SỬA:
backend/src/services/documents/workflow.service.ts
backend/src/services/assets/assetDevice/assetAssignment.service.ts
backend/src/services/excel/excel.service.ts
backend/src/services/documents/documents.validator.ts
backend/src/app.ts
backend/src/routes/auth/auth.routes.ts
backend/.env.example
```

## API Impact

- `POST /api/auths/reset-password` nay có rate-limit RIÊNG tường minh (20 req/15p, giống login/register/refresh-token) — trước đây có rate-limit NGẦM qua mount chung, hành vi thực tế KHÔNG đổi (vẫn 20 req/15p), chỉ đổi từ ngầm→tường minh.
- Không có API contract nào khác thay đổi — toàn bộ phần còn lại là xoá code KHÔNG được gọi tới (theo định nghĩa, không thể có consumer nào bị ảnh hưởng).

## Database Impact

Không có.

## Security Impact

- ARCH-26: không giảm bảo vệ (đã xác nhận `/reset-password` vẫn có rate-limit sau khi hợp nhất, chỉ đổi cơ chế).
- Không có thay đổi security nào khác.

## Tests

| Loại | Kết quả |
|---|---|
| TYPECHECK | **PASSED** — `npx tsc --noEmit` → 0 lỗi (xác nhận không còn tham chiếu nào tới 5 file đã xoá) |
| Full suite | **PASS** — `npx jest` → 14 suite, 84/84 test PASS (không suite nào test riêng các đoạn code đã xoá — đúng bản chất, code chết không có test) |

Không có test mới cho DEV-023 — bản chất thay đổi là XOÁ code không hoạt động + gỡ 1 middleware trùng lặp (hành vi runtime của phần CÒN LẠI không đổi, `npx jest` full suite xác nhận không regression). Riêng route `/reset-password` (đổi rate-limit từ ngầm→tường minh): không có test HTTP thật, nhưng hành vi mong đợi không đổi (vẫn cùng ngưỡng 20 req/15p).

## Review

- `git diff` review từng file — 5 file xoá hẳn, 7 file sửa, đúng phạm vi.
- Đặc biệt kiểm tra kỹ 3 file service lớn (`workflow.service.ts`/`assetAssignment.service.ts`/`excel.service.ts`): xác nhận qua `tail`/`grep` rằng code thật (hàm `syncAssetOnDocumentApproved` với fix `CHECK_DAMAGE` từ DEV-005, `runAssignmentTransaction` mới từ DEV-022, `listImportHistory`/`syncDepartmentFromExcel`) vẫn nguyên vẹn sau khi xoá — `git diff` hiển thị vài dòng `-` rải rác gây hiểu lầm ở đầu diff (thuật toán diff ghép cặp dòng trùng lặp giữa vùng giữ lại và vùng xoá do khối chết là bản sao gần như y hệt) — đã xác minh bằng cách trích xuất chính xác theo SỐ DÒNG (không phải pattern-match nội dung), nên không có rủi ro xoá nhầm.

## Remaining Issues (ghi nhận, KHÔNG tự sửa)

- ARCH-02 (Excel đọc thẳng Department model), ARCH-03 (middleware ghi thẳng UserAudit) — quan sát kiến trúc thuần, không có khuyến nghị hành động cụ thể trong tài liệu gốc, không sửa.
- ARCH-35 (2 cấu hình Multer) — finding gốc tự đánh giá "chấp nhận được ở quy mô hiện tại", không sửa.
- `config/database/database.ts` — 2 đoạn docstring mô tả trùng ý (không phải dead code) — cosmetic, giá trị thấp, không sửa trong task này.
- Không có script/lint rule nào ngăn dead code bị comment thay vì xoá trong tương lai (ARCH-15 tự nhận đây là vấn đề quy trình, không phải 1 lần dọn có thể giải quyết triệt để) — ngoài phạm vi 1 task cleanup.

## Status

**DONE**.
