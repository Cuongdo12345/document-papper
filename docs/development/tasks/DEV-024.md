# DEV-024 — Cân Nhắc Structured Logger (Observability, P3)

> Ngày: 2026-09-02. Status: **DONE** (đánh giá — theo đúng roadmap: "Cân nhắc structured logger, CHỈ khi có kế hoạch scale", và user đã xác nhận **CHƯA có kế hoạch scale**).
> ARCH-30.

## Objective

Toàn bộ hệ thống log qua `console.log/warn/error` trực tiếp (`error.middleware.ts`, `database.events.ts`, và các nơi khác), không có structured logger (winston/pino), không log rotation, không log aggregation. ARCH-30 (Phase 15) ghi nhận đây là hạn chế kiến trúc LOW-MEDIUM, tự nêu rõ điều kiện: **chỉ cấp thiết khi có kế hoạch scale ngang nhiều instance/production observability**. Roadmap giao đúng nhiệm vụ "cân nhắc" (đánh giá + quyết định), không phải "triển khai" mặc định.

## Verify lại trên source hiện tại

| Kiểm tra | Kết quả |
|---|---|
| `error.middleware.ts` | Comment tự thừa nhận: *"đây KHÔNG phải structured logger thực thụ... vẫn dùng `console.error`, chỉ đổi FORMAT"* — xác nhận CONFIRMED, khớp ARCH-30 |
| Log rotation / aggregation (ELK/Loki/Datadog/Sentry) | Không có bất kỳ cấu hình nào trong `package.json`/`backend/src` — CONFIRMED |
| Số lượng instance hiện tại | 1 process Node.js duy nhất (monolith, đã xác nhận ở `15_ARCHITECTURE_REVIEW.md` Mục 2) |
| Kế hoạch scale ngang | **UNKNOWN từ source code** — quyết định nghiệp vụ/hạ tầng, không thể suy ra từ code, phải hỏi người dùng (đúng nguyên tắc §19 CLAUDE.md — business rule chưa rõ → hỏi, không tự giả định) |

## Quyết định của người dùng (AskUserQuestion)

Trình bày 3 lựa chọn (chưa có kế hoạch scale / có kế hoạch scale → thêm pino / chưa chắc, để UNKNOWN) — **người dùng chọn: "Chưa có kế hoạch scale"**.

## Kết luận

- ARCH-30 xác nhận đúng là **CONFIRMED** (100% dùng `console.*`, không có structured logger) — không phải sai lệch so với ghi nhận gốc.
- Điều kiện tự thân của finding ("chỉ cấp thiết khi có kế hoạch scale") **KHÔNG được thoả** ở thời điểm hiện tại (user xác nhận chưa có kế hoạch scale ngang).
- Thêm dependency mới (winston/pino) khi chưa có nhu cầu thật sẽ vi phạm CLAUDE.md §11 (ưu tiên implementation hiện có trước khi thêm dependency) + §12 (không over-engineer, không xây dựng abstraction khi task không cần) + §25 (không cài package chỉ vì convenience, phải giải thích lý do cần thiết — ở đây lý do "cần thiết" chưa tồn tại).
- Quyết định: **giữ nguyên `console.*`**, không thêm dependency, không đổi code — cùng tinh thần với DEV-020 (investigation-only, kết luận không đổi hành vi khi đánh đổi hiện tại vẫn hợp lý ở quy mô hiện tại).

## Changes

**KHÔNG có thay đổi code nào.**

## Affected Files

```
(không có — chỉ đánh giá, không sửa file source nào)
```

## Database / API / Security Impact

Không có (không có thay đổi code).

## Tests

Không áp dụng (không có code thay đổi để test).

## Remaining Issues (ghi nhận, KHÔNG thuộc phạm vi DEV-024)

- Nếu sau này dự án có kế hoạch scale ngang nhiều instance, hoặc cần tổng hợp log xuyên nhiều container/pod để debug production — quay lại ARCH-30 làm baseline, mở lại 1 task riêng để thêm `pino` (nhẹ hơn winston, phù hợp Node.js/Express hiện có) tại các điểm log chính (`error.middleware.ts`, `database.events.ts`).
- Không đổi bất kỳ hành vi log hiện tại nào ở các module khác trong lúc đánh giá task này.

## Status

**DONE** (đánh giá hoàn tất, kết luận: giữ nguyên hành vi hiện tại, không tạo task code tiếp theo trừ khi có kế hoạch scale trong tương lai).
