# FE-02 — RBAC / Permission UI Foundation

> Trạng thái: **DONE** (2026-09-05). Xem `docs/frontend/FE_UI_DEVELOPMENT_ROADMAP.md` Mục 8 (nguồn định nghĩa scope FE-02 — `FE_MASTER_ROADMAP.md` không tồn tại trong project), `docs/frontend/tasks/FE-01.md` (nền tảng trước đó).

## Bối cảnh

Trước khi bắt đầu FE-02, đã audit lại scope theo `FE_UI_DEVELOPMENT_ROADMAP.md` Mục 8 ("FE-02 — RBAC / Permission UI Foundation": `PermissionGuard`, `ProtectedRoute`, `403 page`, `PermissionBadge`, `Permission-aware actions`, `Permission-aware navigation`). Phát hiện: **4/6 mục đã DONE ở FE-01** (`PermissionGuard`, `ProtectedRoute`, `403 page`, `Permission-aware navigation` qua `Sidebar`/`config/navigation.ts`). Đã hỏi user xác nhận scope còn lại trước khi code — user chọn "hoàn tất nốt FE-02 (chỉ thêm PermissionBadge)".

`Permission-aware actions` KHÔNG có gì để áp dụng ở FE-02 vì chưa có action button thật nào (Users/Documents/Departments vẫn là `PlaceholderPage`, thuộc scope FE-03+) — ghi nhận, không tự bịa page để "áp dụng cho có".

## Đã implement

1. `frontend/src/utils/humanizePermission.ts` (**mới**) — pure function format permission string kỹ thuật thành dạng dễ đọc (`"USER_CREATE"` → `"User create"`). **CHỈ format chữ hoa/underscore, KHÔNG dịch nghĩa tiếng Việt** — backend hiện không có bảng mô tả (`description`) cho từng permission (`SHARED_COMPONENTS_LIBRARY.md`), tự bịa nghĩa cho ~75 permission là suy diễn business copy không có evidence (CLAUDE.md Mục 19 — Business Logic phải CONFIRMED/INFERRED/UNKNOWN).
2. `frontend/src/components/auth/PermissionBadge.tsx` (**mới**) — compose lại `StatusBadge` đã có (KHÔNG tự viết CSS pill riêng, đúng "Quy tắc chống drift" Mục 31 roadmap: không duplicate shared component). Prop `permissionName: string`, hiển thị bản format qua `humanizePermission()`, giữ nguyên permission string gốc qua `title` (tooltip) để không mất thông tin kỹ thuật.
3. `frontend/src/components/shared/StatusBadge.tsx` — sửa nhỏ: thêm optional prop `title` (passthrough native tooltip), dùng bởi `PermissionBadge`. Không đổi behavior/API hiện có (additive, backward-compatible — 2 nơi đang dùng `StatusBadge` khác, `DashboardPage.tsx`/`Header.tsx`, không bị ảnh hưởng).
4. `frontend/src/utils/__tests__/humanizePermission.test.ts` (**mới**) — 5 unit test (2 từ, 3 từ, 1 từ, không có underscore, chuỗi rỗng).

## Các file đã thay đổi

- `frontend/src/utils/humanizePermission.ts` (mới)
- `frontend/src/utils/__tests__/humanizePermission.test.ts` (mới)
- `frontend/src/components/auth/PermissionBadge.tsx` (mới)
- `frontend/src/components/shared/StatusBadge.tsx` (sửa — thêm prop `title`)

Không sửa `usePermission.ts`/`PermissionGuard.tsx`/`ProtectedRoute.tsx`/`config/navigation.ts` (đã đúng từ FE-01, không cần đổi). Không tạo trang RBAC Admin thật (Roles/Permissions UI — thuộc FE-16, chưa tới scope).

## Kết quả kiểm thử

| Test | Kết quả |
|---|---|
| Typecheck | **PASS** |
| Lint | **PASS** (1 warning pre-existing không liên quan, từ FE-00) |
| Unit tests | **PASS** — 16/16 (11 cũ + 5 mới `humanizePermission`) |
| Build | **PASS** — 585.95 kB / gzip 188.00 kB (tăng không đáng kể so với FE-01 hiện tại) |

`git status --porcelain backend` không đổi trong suốt task (backend không bị đụng tới).

## Known Issues / Chưa làm (đúng scope, không phải thiếu sót)

1. `PermissionBadge` chưa có nơi dùng thật (RBAC Admin — Roles/Permissions UI — là FE-16, chưa tới) — component đã sẵn sàng reuse khi tới lúc.
2. "Permission-aware actions" (ẩn/disable action button theo permission) chưa có ví dụ áp dụng thật vì chưa có action button nào ngoài placeholder — `PermissionGuard` (đã có từ FE-01) sẽ dùng trực tiếp khi FE-03 code action thật (Create/Edit/Delete...).
3. Bảng dịch permission → tiếng Việt (cho non-technical user, vd trang RBAC Admin) vẫn là **UNKNOWN/pending** — cần backend bổ sung field `description` hoặc 1 nguồn dữ liệu chính thức khác trước khi FE có thể hiển thị nghĩa thật thay vì chỉ format chuỗi kỹ thuật.

## FE-03 Readiness

**READY** — RBAC UI Foundation (FE-02) đã đủ 6/6 mục theo roadmap. `PermissionGuard`/`PermissionBadge`/`usePermission()` sẵn sàng reuse trực tiếp khi FE-03 code trang Users/Departments thật.
