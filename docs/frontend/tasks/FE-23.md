# FE-23 — Trang "System Design" (bản đồ module + quan hệ dữ liệu, chỉ dev/admin)

**Ngày:** 2026-09-21
**Trạng thái:** DONE HOÀN TOÀN — code, DB dev (`DEV-072.md` Mục 4), và verify trình duyệt thật đều xong.
User đã tự mở `/app/system-design` xác nhận chạy được (phản hồi trực tiếp trong chat, 2026-09-21).
**Phạm vi:** Frontend — dữ liệu module tĩnh + trang hiển thị + route + nav item. Phần permission backend xem
`docs/development/tasks/DEV-072.md`.

## 1. Yêu cầu

Đọc `docs/04_DATABASE_ANALYSIS.md` + `docs/02_ARCHITECTURE.md` (đã cập nhật đủ 31 model, DEV-071), tạo:
1. Permission mới, gán role phù hợp (→ `DEV-072.md`).
2. File dữ liệu liệt kê từng module (tên, mô tả, field chính, quan hệ) — SINH RA từ 2 file docs trên.
3. Trang FE hiển thị, nhóm theo domain, có đường nối/nhãn quan hệ đơn giản bằng HTML/CSS — KHÔNG thêm thư
   viện diagram (Mermaid/React Flow...).
4. Route + permission gate + nav item, nhóm "QUẢN TRỊ HỆ THỐNG" (FE-20).

## 2. File dữ liệu — `frontend/src/features/systemDesign/data/systemModules.ts`

`SYSTEM_MODULES: SystemModule[]` — **31 entry, khớp đúng 4.1→4.31 của `04_DATABASE_ANALYSIS.md`**, nhóm theo
12 domain đúng bảng ở `02_ARCHITECTURE.md` §4 (Users/Auth/RBAC/Departments/Documents/Assets/Vendors/
Inventory/Notifications/Upload/Performance/Import Audit). Mỗi entry: `id`, `domain`, `name`, `description`
(1 câu, dịch từ mô tả trong doc), `keyFields[]` (3-8 field chính, lấy từ bảng field trong doc — KHÔNG liệt
kê hết mọi field), `relations[]` (`{to, label}`, lấy từ Mục 6 "Relationships" của `04_DATABASE_ANALYSIS.md`),
`sourceFile` (đường dẫn model thật, làm evidence — không phải link bấm được).

**SINH RA từ docs, không tự đọc lại source backend** — đúng yêu cầu user. Vài chi tiết đáng chú ý được giữ
nguyên từ doc gốc (không đơn giản hoá mất): `WorkflowInstance` không có index nào dù bị query nhiều nhất;
`ConsumableItem` unique compound `{department, name}`; `DocumentPdfExport.signature` KHÔNG PHẢI chữ ký số CA
hợp lệ pháp lý; `Vendor`/`Permission`/`Policy`/`Department`/`Counter`/`WorkflowTemplate` không có `relations`
ra module khác (đúng thực tế trong doc, không tự bịa quan hệ để "đẹp sơ đồ").

`getModuleById(id)` — helper tra cứu module theo id, dùng để resolve tên hiển thị + kiểm tra tồn tại khi
render quan hệ (relation trỏ tới domain chưa khai báo sẽ tự ẩn phần link mà không lỗi runtime).

## 3. Trang FE — `frontend/src/features/systemDesign/pages/SystemDesignPage.tsx`

- Dùng `PageHeader` (shared component có sẵn) — đúng Design System Rule, không tự tạo tiêu đề trang riêng.
- Nhóm theo domain (`SYSTEM_DOMAINS` thứ tự cố định), mỗi domain 1 `<section>` với heading + số lượng model,
  bên dưới là grid card (`sm:grid-cols-2 xl:grid-cols-3`).
- Mỗi module là 1 card (`bg-card`, `border-border` — đúng token màu sẵn có trong `index.css`, không tự đặt
  màu mới): tên, đường dẫn file model, mô tả, chip field chính, danh sách quan hệ.
- **"Đường nối quan hệ" — thuần HTML/CSS, KHÔNG thêm thư viện diagram**: danh sách quan hệ dùng
  `border-left` của `<ul>` + 1 gạch ngang nhỏ (`::before`-style qua `<span>` tuyệt đối định vị) trước mỗi
  item, giống UI cây thư mục — kỹ thuật CSS thuần rất phổ biến, không cần đo `getBoundingClientRect`/vẽ SVG.
  Click vào tên module trong danh sách quan hệ → `scrollIntoView({block:"center"})` (KHÔNG dùng `smooth` —
  giữ instant, tránh thêm hiệu ứng chuyển động mới ngoài chủ đích) + viền `ring-2 ring-primary` cho card đích
  (state tức thời, không phải animation/fade) — tuân thủ đúng
  `docs/frontend/UI_DESIGN_SYSTEM.md` §5 ("không thêm hover/fade-in mới", ngoại lệ DUY NHẤT là Toaster).
- Có thanh chuyển nhanh domain ở đầu trang (chip link neo `#domain-...`, thuần `<a href>`, không JS).
- Không có loading/error state — trang thuần tĩnh, không gọi API nào.

## 4. Route + Permission gate + Nav item

- `frontend/src/constants/permissions.ts` — thêm `SYSTEM_DESIGN_VIEW` (mirror đúng backend).
- `frontend/src/routes/index.tsx` — thêm block `ProtectedRoute permission={PERMISSIONS.SYSTEM_DESIGN_VIEW}`,
  route con `{ path: "system-design", element: <SystemDesignPage /> }` (đặt cuối danh sách route trong
  `/app`, sau "files" — không chèn giữa để không xáo trộn route khác).
- `frontend/src/config/navigation.ts` — thêm `NavItem` mới cuối mảng `NAV_ITEMS`, `group: "Quản trị hệ
  thống"` (đúng yêu cầu — nối tiếp dãy liền kề "Quản trị hệ thống" đã có từ FE-20/21/22, KHÔNG chèn giữa để
  tránh xáo trộn thứ tự FE-22 vừa chỉnh), icon `Network` (lucide-react, đã có sẵn trong dependency, không
  thêm icon set mới).

## 5. Verify

- `npx tsc --noEmit` (frontend) — 0 lỗi.
- `npm run build` (frontend) — PASS (chunk-size warning là pre-existing, không liên quan thay đổi này).
- `npm run lint` (oxlint) — PASS, 4 warning còn lại đều pre-existing (file khác, không phải file mới thêm).
- `npm run test` (vitest) — **4 file / 25 test PASS** (không có test nào liên quan trực tiếp trang mới —
  FE-17 a11y/visual regression KHÔNG chạy lại vì trang mới CHƯA thể truy cập được tới khi DB dev có
  permission — xem Mục 6).
- DB dev đã có permission thật (`DEV-072.md` Mục 4, verify qua `GET /users/me` — `admin` có
  `SYSTEM_DESIGN_VIEW` trong 111 permission trả về).
- **User đã tự mở trình duyệt xác nhận chạy được** (2026-09-21, phản hồi trực tiếp trong chat: "ok đã chạy
  được nhé") — nav item, route, trang đều hiển thị đúng.

## 6. Việc CẦN LÀM THÊM

- Không còn bước bắt buộc nào — task DONE hoàn toàn.
- Tuỳ chọn (không bắt buộc): chạy lại bộ a11y/visual regression (FE-17, `frontend/e2e/`) cho trang mới nếu
  muốn đưa vào bộ test thường trực — task này không yêu cầu, chỉ ghi nhận làm tuỳ chọn cho phiên sau.
