# FE-22 — Gộp "Thông báo"/"Tệp tin" vào nhóm "Quản trị hệ thống"

**Ngày:** 2026-09-20
**Trạng thái:** DONE
**Phạm vi:** CHỈ `frontend/src/config/navigation.ts` (không đụng `Sidebar.tsx` — logic gom nhóm theo dãy
liền kề đã có sẵn từ FE-20/21, chỉ cần đổi vị trí + `group` của 2 mục). Yêu cầu trực tiếp từ user (kèm ảnh
chụp Sidebar sau FE-21), tiếp nối FE-20/FE-21.

## 1. Thay đổi

- **Vấn đề**: Sidebar nhóm theo DÃY LIỀN KỀ trong `NAV_ITEMS` (không tự sắp xếp lại lúc render, đúng ràng
  buộc "không đổi thứ tự lọc" từ FE-20). "Thông báo" đứng ngay sau "Duyệt tài liệu" (giữa cụm "Tài liệu"),
  "Tệp tin" đứng cuối cùng — cả 2 không `group`. Chỉ đổi `group: "Quản trị hệ thống"` cho 2 mục mà GIỮ
  NGUYÊN vị trí sẽ tạo ra 1 khối "QUẢN TRỊ HỆ THỐNG" thứ 2 tách rời (Sidebar không gộp 2 khối cùng tên nếu
  không liền kề) — SAI ý đồ user.
- **Đã sửa**: dời `Thông báo`/`Tệp tin` xuống vị trí NGAY SAU "Nhật ký audit" (cuối cụm "Quản trị hệ
  thống"), gán `group: "Quản trị hệ thống"` cho cả 2 — giờ nằm liền kề, Sidebar tự gộp thành 1 khối duy
  nhất. KHÔNG đổi `path`/`permission` của cả 2 mục.
- **Ghi đè ràng buộc cũ có chủ đích**: comment gốc ở `NavItem.group` (FE-20) từng nói "không đổi thứ tự" —
  task này CHỦ ĐỘNG đổi thứ tự theo đúng chỉ định mới của user, đã cập nhật comment giải thích rõ lý do
  (không để lại tài liệu mâu thuẫn với source, đúng CLAUDE.md §3/§29).
- Cập nhật comment tại 2 mục: giữ nguyên phần giải thích domain gốc (Thông báo là self-scoped/mọi user đều
  thấy — KHÔNG phải tính năng chỉ ADMIN; Tệp tin là thư viện độc lập không gắn Document/Asset), chỉ thêm
  ghi chú "đã dời vị trí + lý do" — không xoá thông tin domain vẫn còn đúng.

## 2. Verify

- **Tự chụp Playwright tạm** (viết rồi XOÁ sau khi xem): xác nhận đúng — "Thông báo"/"Tệp tin" nằm cuối
  khối "QUẢN TRỊ HỆ THỐNG", không tạo khối trùng tên nào khác.
- `npm run build` (frontend) — PASS.
- `npx vitest run` — 25/25 PASS.
- `npx oxlint` trên `navigation.ts` — sạch.
- **FE-17 regression đầy đủ**: a11y Dashboard PASS (3 màn còn lại vẫn CÙNG finding cũ đã biết, không liên
  quan), **visual cả 4 test PASS**, không cần cập nhật baseline.
- `git status` xác nhận đúng 1 file source thay đổi (`navigation.ts`).

## 3. Cập nhật tài liệu

- `docs/frontend/FRONTEND_MEMORY.md`, `docs/SESSION_HANDOFF.md` — thêm entry trỏ tới FE-22.

## 4. Việc CẦN LÀM THÊM

Không có.
