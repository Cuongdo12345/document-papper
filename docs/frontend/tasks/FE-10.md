# FE-10 — Asset Categories UI

**Ngày:** 2026-09-09
**Trạng thái:** DONE
**Phạm vi:** `FE_UI_DEVELOPMENT_ROADMAP.md` Mục 14 — phần còn thiếu của FE-06
(FE-06 chỉ làm List/Detail/Create/Edit/Delete/Assign/Transfer/Return cho
**Asset**, để lại CRUD đầy đủ cho **AssetCategory** như 1 task riêng — xem
comment gốc `assetCategories.api.ts` FE-06 + `FRONTEND_MEMORY.md` Mục 4/5).

---

## 1. Trước khi code — audit hiện trạng

- `useAssetCategories.ts`/`assetCategories.api.ts` (FE-06) CHỈ có `getAssetCategories` — dùng cho dropdown chọn danh mục ở Create/Edit Asset, KHÔNG có create/update/delete/restore.
- Backend đã có ĐẦY ĐỦ 7 route (`assetCategory.routes.ts`): `POST /`, `GET /`, `GET /:id`, `PUT /:id`, `DELETE /:id` (soft), `DELETE /:id/permanent` (hard), `PATCH /:id/restore`.
- **Phát hiện gap giống hệt DEV-035 (Assets)**: `getAllAssetCategoriesService` HARD-CODE `filter.isActive = true`, `QueryAssetCategoryDTO` không có field `isActive` — nghĩa là dù `PATCH /:id/restore` đã tồn tại sẵn, KHÔNG CÓ CÁCH NÀO liệt kê lại danh mục đã xoá mềm để gọi tới nó. Cùng module (Assets), cùng nguyên nhân, cùng cách sửa đã có tiền lệ ĐƯỢC PHÊ DUYỆT ở DEV-035 — xử lý trong task này luôn (không tách task riêng), xem Mục 2.
- `ASSET_CATEGORY_DELETE_PERMANENT` — xác nhận qua `rolePermission.map.ts`: **CỐ TÌNH không gán cho bất kỳ role nào** (comment gốc "rủi ro cao", cùng lý do `ASSET_DELETE_PERMANENT`). Đối chiếu FE-06: `ASSET_DELETE_PERMANENT` cũng 0 UI (`grep` toàn bộ frontend — không có file nào dùng). Theo đúng tiền lệ, task này KHÔNG build UI "Xoá vĩnh viễn" cho AssetCategory.
- `ASSET_CATEGORY_UPDATE_WHITELIST` (`assets.constants.ts`) = `["name","parentCategory","defaultWarrantyMonths","isActive"]` — **`code` KHÔNG có trong whitelist** → mã danh mục bất biến sau khi tạo, form Sửa phải khoá field này (khác form Tạo).
- **Phát hiện phụ, ngoài scope UI**: `UpdateAssetCategoryDTO` cho phép set `isActive` trực tiếp qua `PUT /:id` — nhưng đường này BỎ QUA 2 check an toàn mà `deleteAssetCategoryService` có (chặn nếu còn tài sản/danh mục con tham chiếu). Đây là khoảng hở business logic ở BACKEND (không phải bug FE) — **FE chủ động không bao giờ gửi `isActive` qua form Sửa** (chỉ đổi trạng thái qua 2 action Xoá/Khôi phục có check đầy đủ), tránh tự tạo thêm đường vòng qua lỗ hổng này. Ghi nhận, không tự sửa backend (ngoài phạm vi UI task, theo CLAUDE.md Mục 26 — No Blind Refactoring).

## 2. Backend — thay đổi additive tối thiểu (tiền lệ DEV-035)

| File | Thay đổi |
|---|---|
| `backend/src/dto/assets/assets.dto.ts` | `QueryAssetCategoryDTO` thêm `isActive` (enum `"true"/"false"` → boolean/undefined, CÙNG PATTERN CHÍNH XÁC `QueryAssetDTO.isActive`) |
| `backend/src/services/assets/assetDevice/assetCategory.service.ts` | `getAllAssetCategoriesService`: `filter.isActive` đổi từ hard-code `true` → `isActive === undefined ? true : isActive` |
| `backend/src/docs/openAPI.yaml` | (a) Thêm param `isActive` vào `GET /api/assets/asset-categories`. (b) **Sửa 1 lỗi tài liệu SẴN CÓ TRƯỚC task này**: schema `AssetCategory.parentCategory` từng khai `ObjectIdRef` thô — SAI, source (`assetCategory.service.ts`) xác nhận `GET /`/`GET /:id` LUÔN `.populate("parentCategory","code name")`, response thật là object. Sửa thành `oneOf: [ObjectIdRef, {_id,code,name}]` (cùng style `Asset.category`/`Asset.department` đã dùng sẵn trong file), kèm giải thích khi nào là ID thô (request body) khi nào là object (response). |

Không đổi request/response shape của bất kỳ endpoint nào khác. Không đổi
DTO tạo/sửa (`CreateAssetCategoryDTO`/`UpdateAssetCategoryDTO`/whitelist).

## 3. Frontend — file mới/sửa

**Mới:**
- `frontend/src/features/assets/pages/AssetCategoriesListPage.tsx` — List + filter (keyword, isActive) + Pagination + rowActions (Sửa/Xoá khi active, Khôi phục khi đã ẩn). KHÔNG có cột sort (`QueryAssetCategoryDTO` không hỗ trợ `sortBy`, backend luôn sort cố định theo `code` — không tự bịa khả năng UI mà API không có).
- `frontend/src/features/assets/components/AssetCategoryFormModal.tsx` — modal Tạo/Sửa dùng chung 1 Zod schema (bài học FE-04 `DocumentMetaFields`: 2 schema riêng dễ lệch type). `code` khoá (hidden input giữ giá trị gốc, cùng pattern field `category` bị khoá ở `AssetEditModal.tsx`) khi Sửa.
- `frontend/src/features/assets/components/AssetSectionTabs.tsx` — sub-nav 2 tab "Tài sản"/"Danh mục tài sản" (CÙNG PATTERN `RbacSectionTabs.tsx` FE-08) — dùng thay vì thêm mục sidebar cấp cao mới cho 1 sub-resource (CLAUDE.md Mục 20: không đổi global layout nếu task không yêu cầu).
- `frontend/src/features/assets/hooks/useCreateAssetCategory.ts`, `useUpdateAssetCategory.ts` (form — không toast lỗi trong hook), `useDeleteAssetCategory.ts`, `useRestoreAssetCategory.ts` (quick action — toast trong hook). Đúng quy ước lỗi mutation đã chốt từ FE-03.

**Sửa:**
- `frontend/src/types/asset.types.ts` — `AssetCategory.parentCategory` đổi type từ `string | null` → `{_id,code,name} | null` (khớp response thật đã populate, xem Mục 2); thêm `GetAssetCategoriesParams.isActive`, `CreateAssetCategoryRequest`, `UpdateAssetCategoryRequest`. Không phá vỡ consumer cũ (`AssetCreatePage`/`AssetEditModal`/`AssetsListPage` filter dropdown chỉ đọc `_id`/`name`, không đụng `parentCategory`).
- `frontend/src/api/assetCategories.api.ts` — thêm `getAssetCategoryById`/`createAssetCategory`/`updateAssetCategory`/`deleteAssetCategory`/`restoreAssetCategory` (trước chỉ có `getAssetCategories`).
- `frontend/src/features/assets/pages/AssetsListPage.tsx` — chèn `<AssetSectionTabs />` dưới `PageHeader` để 2 trang liên kết qua lại.
- `frontend/src/routes/index.tsx` — thêm route `assets/categories`, gate RIÊNG bằng `ASSET_CATEGORY_VIEW` (khác `ASSET_VIEW` của nhóm cha `assets/`) — đúng nguyên tắc mỗi resource RBAC tự gate permission của chính nó (cùng cách `rbac/{roles,permissions,policies}` ở FE-08), không dựa vào việc 2 permission này hiện luôn đi cùng nhau trong `rolePermission.map.ts` (dữ liệu có thể đổi sau).

Không thêm sidebar item mới (`config/navigation.ts` không đổi) — điều
hướng qua `AssetSectionTabs`, xem lý do ở trên.

## 4. Verify

- `backend`: `npx tsc --noEmit` — PASS, 0 lỗi. `openAPI.yaml` parse thử bằng `js-yaml` — PASS (cú pháp hợp lệ).
- `frontend`: `npm run typecheck` — PASS. `npm run build` — PASS (chỉ còn cảnh báo chunk-size pre-existing, không phải lỗi mới). `npx oxlint` — PASS, đúng 2 warning pre-existing y hệt baseline các task trước (`CalibrationStatusBadge.tsx`, `button.tsx`), 0 warning mới.
- **CHƯA verify bằng HTTP/browser thật** (không tạo/sửa/xoá dữ liệu dev ngoài yêu cầu, chủ động không tự ý thao tác DB) — chỉ verify qua đọc source đối chiếu DTO/service/route kỹ (evidence-based, xem Mục 1-2) + build/typecheck/lint. Nếu cần xác nhận runtime, khuyến nghị người dùng tự thử 1 lượt Tạo/Sửa/Xoá/Khôi phục trên `/app/assets/categories`.

## 5. Remaining / ngoài scope (không làm ở task này)

- Backend business-logic gap: `PUT /:id` với `isActive:false` bỏ qua 2 check an toàn của `deleteAssetCategoryService` (Mục 1) — chỉ ghi nhận, không tự sửa (ngoài phạm vi UI task).
- "Xoá vĩnh viễn" (`ASSET_CATEGORY_DELETE_PERMANENT`) — không có UI, đúng tiền lệ `ASSET_DELETE_PERMANENT` (FE-06).
- Excel import/QR code cho Assets (nhắc tới chung với "Asset Categories" ở `FRONTEND_MEMORY.md` Mục 5 cũ) — xác nhận qua source: KHÔNG liên quan Asset Category (Excel hiện chỉ phục vụ domain Document ở `excel.route.ts`; QR code là route riêng theo TỪNG Asset `/assets/:id/qrcode`, không phải theo Category) — 2 việc này KHÔNG thuộc phạm vi FE-10, để task riêng sau nếu được yêu cầu.
