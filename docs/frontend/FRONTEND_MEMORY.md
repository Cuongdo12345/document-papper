# FRONTEND MEMORY

> Index sống, cập nhật liên tục cho quá trình phát triển FE — cùng vai trò với `docs/00_PROJECT_MEMORY.md` nhưng chỉ phạm vi Frontend. KHÔNG phải bản sao `API_REFERENCE.md`/`UI_REQUIREMENTS.md`/các file KB khác.

---

## 1. Current FE status (2026-09-03)

- **Frontend CHƯA TỒN TẠI trên đĩa** — chưa có thư mục `frontend/`, chưa cài dependency nào, chưa viết dòng code FE nào.
- Đã hoàn thành: **UI-00** (phân tích + lập roadmap ban đầu) và **Frontend Knowledge Base task hiện tại** (bộ 13 tài liệu `docs/frontend/`) — cả 2 đều CHỈ phân tích/tài liệu, KHÔNG code.
- **Ghi chú lịch sử**: 4 file tạo ở UI-00 (`00_FRONTEND_ROADMAP.md`, `UI_DESIGN_SYSTEM.md`, `API_LAYER_SPEC.md`, `FRONTEND_MEMORY.md` bản cũ) đã bị **user chủ động xoá** để làm lại phân tích từ đầu (xác nhận trực tiếp từ user) — không phải sự cố/lỗi hệ thống. Nội dung của 4 file đó (roadmap FE-00→18, design token màu/typography) **KHÔNG nằm trong bộ tài liệu hiện tại** (phạm vi task Frontend Knowledge Base không yêu cầu 2 loại tài liệu này) — **nếu cần roadmap theo giai đoạn (FE-00→FE-18) hoặc design system (màu/typography/component visual spec), cần yêu cầu 1 task riêng**, không tự suy đoán đã tồn tại.
- Backend: 25/25 development task DONE, xem `docs/30_DEVELOPMENT_COMPLETION_AUDIT.md` (kết luận: DEVELOPMENT COMPLETE WITH OPEN RISKS).
- **FE FOUNDATION SPECIFICATION hoàn thành** (`FE_FOUNDATION_SPEC.md`) — cầu nối chính thức KB → coding contract (stack/folder/API layer/response normalization/error/auth/RBAC/routing/state/React Query/TypeScript/forms/UI/table/filter/upload/dashboard/notification/security/env/naming/coding rules/testing/observability/DoD).
- **FE-00 — Frontend Project Bootstrap: DONE (2026-09-05)**. Foundation ban đầu (axios/authStore/router/Tailwind+shadcn cơ bản) — xem lịch sử ở `docs/frontend/tasks/FE-00.md`.
- **DEV-026 (backend) — RBAC Micro-Fix: DONE** — `GET /users/me` giờ trả `role.isSystemRole` + `permissions: string[]` (effective) thật.
- **FE-01 — Authentication + App Shell + UI Foundation: DONE (2026-09-05)**. Chi tiết đầy đủ: `docs/frontend/tasks/FE-01.md`. Tóm tắt:
  - `usePermission()` ĐÃ SỬA — đọc thẳng `user.permissions` (logic thuần tách riêng ở `utils/permissionCheck.ts`, có unit test), KHÔNG còn bypass ADMIN cứng, KHÔNG tự tính effective permission.
  - `PermissionGuard` (`components/auth/`) + `ProtectedRoute`/`PublicRoute`/`RootRedirect` (routing đầy đủ, xử lý loading state tránh flash 403 sai khi user chưa load xong).
  - App Shell thật: `Sidebar` (collapse desktop + drawer mobile, nav lọc theo permission), `Header` (user menu dropdown, avatar, role badge, logout), `AppLayout` ghép cả 2 + responsive.
  - Design system Modern Healthcare Enterprise: tokens xanh dương/teal (`index.css`, thay neutral mặc định shadcn của FE-00), font Inter, 8 shared component (`PageHeader`/`StatusBadge`/`LoadingState`/`EmptyState`/`ErrorState`/`AppModal`/`AppDrawer`/`Toaster`+`toastStore`).
  - Route thật: `/login` (public), `/` (redirect), `/app` (dashboard placeholder) + `/app/documents|users|departments|audit-logs` (permission-gated placeholder), `/403`, `/404`.
  - Test mới: Vitest (mới cài) + 11 unit test (`permissionCheck`, `parseApiError`) PASS. `typecheck`/`build`/`lint` PASS.
  - **Known limitation KHÔNG đổi từ FE-00**: chưa verify được luồng login 200 thật bằng request/browser thật (thiếu tài khoản test, chủ động không tự tạo user trong DB thật).
- **FE-01 MICRO-FIX — Session Restore: PASS (2026-09-05)**. Chi tiết: `docs/frontend/phases/FE-01_MICRO_FIX_SESSION_RESTORE.md`. Tóm tắt (quyết định kiến trúc lâu dài):
  - **Bug thật đã CONFIRMED** (không phải giả thuyết): `ProtectedRoute` redirect `/login` đồng bộ ngay khi `accessToken` in-memory rỗng sau reload, TRƯỚC khi có cơ hội dùng `refreshToken` (localStorage) — vì `useCurrentUser()` bị `enabled:false` nên không có request nào bắn ra để kích hoạt refresh-lock 401. Dòng giả định cũ ở `FE-01.md` Mục Session restore ("refresh-lock tự xử lý transparent ở request đầu tiên") là **SAI**, đã sửa.
  - **Session bootstrap strategy (mới, áp dụng lâu dài)**: `hooks/useAuthBootstrap.ts` chạy 1 lần lúc `App.tsx` mount — nếu có `refreshToken` (localStorage), gọi `refreshAccessToken()` (tái sử dụng nguyên hàm có sẵn ở `axios.ts`, nay export) TRƯỚC khi `RouterProvider` render. `authStore` có thêm field `isInitializing` (default `true`) — `App.tsx` render `LoadingState` fullscreen thay vì router cho tới khi bootstrap xong, tránh flash `/login` sai.
  - Token storage strategy KHÔNG đổi (`accessToken` in-memory, `refreshToken` localStorage) — chỉ thêm bước dùng `refreshToken` sớm hơn (lúc app khởi động thay vì chỉ lúc 401).
  - Verify runtime: dùng tài khoản `admin` có sẵn trong DB dev (không tạo mới) — xác nhận qua `curl` toàn bộ chuỗi `login → refresh-token → GET /users/me` đúng contract. Chưa có browser automation tool để tự bấm F5 quan sát React runtime thật — khuyến nghị người dùng tự xác nhận 1 lần trên browser thật.
- **Bug fix (2026-09-05, phát hiện khi test thật trên browser)**: `axios.ts` response interceptor coi MỌI lỗi 401 (kể cả từ chính `POST /auths/login` khi sai mật khẩu) là "accessToken hết hạn" và tự thử refresh — khi chưa từng có `refreshToken` (lần login đầu), `refreshAccessToken()` throw 1 `Error` JS thường (không phải `AxiosError`), khiến `parseApiError()` hiểu nhầm thành network error, hiện sai "Không thể kết nối máy chủ" thay vì "Tên đăng nhập hoặc mật khẩu không đúng". **Đã sửa**: loại trừ `/auths/login` khỏi auto-refresh (cùng pattern với `/auths/refresh-token` đã có). Cũng đồng bộ lại `loginSchema` (Zod) ở `LoginPage.tsx` khớp đúng `LoginDTO` backend (username min 3, password min 5) — trước đó chỉ check non-empty nên giá trị quá ngắn vượt validate client, gây hiện lỗi generic thay vì message rõ ràng tại field.
- **Forgot Password / Reset Password: DONE (2026-09-05)** — 2 trang còn thiếu từ FE-01 (`AuthLayout.tsx` đã có comment dự trù từ trước), nay đã code đủ theo đúng `AUTH_RBAC_MAP.md` Mục 1.2/1.3: `features/auth/pages/ForgotPasswordPage.tsx` (`/forgot-password`) + `ResetPasswordPage.tsx` (`/reset-password`, đọc `token` từ query string), `features/auth/hooks/useForgotPassword.ts`/`useResetPassword.ts`, `api/auth.api.ts` bổ sung `forgotPassword()`/`resetPassword()`, `types/auth.types.ts` bổ sung `ForgotPasswordRequest`/`ResetPasswordRequest`. Zod schema khớp đúng `ForgotPasswordDTO`/`ResetPasswordDTO` backend (`newPassword` min **8**, khác `LoginDTO.password` min 5 — đã phân biệt đúng, không nhầm 2 ngưỡng). Forgot Password hiển thị ĐÚNG NGUYÊN message trung tính từ backend (không tự suy diễn), verify qua curl: username tồn tại/không tồn tại trả về CÙNG 1 message. Login page có thêm link "Quên mật khẩu?".
- **FE-02 — RBAC / Permission UI Foundation: DONE (2026-09-05)**. Chi tiết: `docs/frontend/tasks/FE-02.md`. Audit trước khi code phát hiện 4/6 mục scope (theo `FE_UI_DEVELOPMENT_ROADMAP.md` Mục 8) đã DONE ở FE-01 (`PermissionGuard`/`ProtectedRoute`/`403 page`/`Permission-aware navigation`) — chỉ còn thiếu `PermissionBadge` (đã code, compose lại `StatusBadge`, dùng `utils/humanizePermission.ts` format hiển thị KHÔNG dịch nghĩa vì backend chưa có bảng mô tả permission). "Permission-aware actions" chưa có nơi áp dụng thật (chưa có action button ngoài placeholder) — ghi nhận, không bịa page để áp dụng cho có.
- **FE-03 — Users / Departments UI thật: DONE (2026-09-05)**. Chi tiết: `docs/frontend/tasks/FE-03.md`. Trước khi code, phát hiện 2 blocker xử lý riêng ở `docs/development/tasks/DEV-027.md`: (1) bug nghiêm trọng `validateQuery` middleware crash 500 MỌI route dùng nó (Express 5, `req.query` accessor getter-only — đã fix + test hồi quy); (2) role `IT` được cấp thêm `USER_VIEW`/`USER_VIEW_DETAIL` (chỉ xem, theo yêu cầu user, áp dụng qua API RBAC chứ không chạy seed script). Tóm tắt kỹ thuật:
  - `GET /users`/`GET /users/:id` **KHÔNG populate `role`** (chỉ ObjectId thô, khác `/users/me`) — FE tự resolve tên qua `GET /rbac/roles` (`features/rbac/hooks/useRoles.ts`).
  - 4 shared component mới: `DataTable` (server-side sort, skeleton/empty/error state), `Pagination`, `FilterBar` (layout wrapper nhẹ, KHÔNG phải config-engine — tránh over-engineer khi mới 2 domain dùng), `ConfirmDialog` (compose `AppModal`).
  - Quy ước lỗi mutation: gắn **form** (Create/Update) → KHÔNG toast trong hook, component tự đọc `parseApiError(mutation.error)` hiện inline; gắn **quick action** (assign role/disable/restore/reset password/delete — nút+ConfirmDialog, không form) → toast trong hook (giống `useLogout.ts`).
  - `department.model.ts` xác nhận qua source **CHỈ CÓ `code`+`name`** — `UI_REQUIREMENTS.md` từng suy đoán sai có `description`/`isActive`.
  - Response shape Users/Departments KHÔNG đồng nhất NGAY TRONG CÙNG 1 CONTROLLER (`GET /departments` dùng `success`, 4 endpoint Department khác dùng `message`; `GET /users` dùng `message` nhưng giá trị `true` — boolean) — đã nới lỏng type `unwrapResponse.ts` (`message?: unknown`), không đổi runtime.
  - Mutation flows (create/update/delete) **NOT VERIFIED qua HTTP thật** (chủ động không tạo/sửa/xoá dữ liệu dev) — chỉ verify GET (read-only) + review code kỹ so khớp DTO.
- **DEV-028 (backend) — bổ sung `GET /workflows/templates`: DONE** — trước đó chỉ có `POST /workflows/templates` (tạo), không có cách liệt kê lại — chặn action Submit vào workflow ở FE-04. Hỏi user qua `AskUserQuestion`, được xác nhận bổ sung endpoint (đọc-only, permission `WORKFLOW_VIEW` dùng lại).
- **FE-04 — Documents Core UI: DONE (2026-09-05)**. Chi tiết: `docs/frontend/tasks/FE-04.md`. Tóm tắt:
  - Domain trung tâm nhất, quy mô lớn nhất từ trước tới giờ: List (filter/sort đầy đủ)/Create (Proposal, 3 subType)/Detail (view/edit/xoá/khôi phục/submit workflow)/xoá theo tháng (ADMIN-only ở UI).
  - `meta` shape THEO SUBTYPE xác nhận bằng DỮ LIỆU THẬT (không suy đoán): `PROPOSE_REPAIR`→`{issue}`; `PROPOSE_INK`/`PROPOSE_PROCUREMENT`→`{items[],totalAmount}`; `CHECK_DAMAGE`/`CONFIRM_STATUS`→`{inspectionResult,items[],totalAmount}`; `MANUAL` không có mẫu thật (fallback JSON thô).
  - **2 rule ẩn CONFIRMED thêm** (chưa từng ghi trong `DOCUMENT_DOMAIN_MAP.md` cũ): (1) `deleteDocumentService` chặn 403 nếu KHÔNG PHẢI ADMIN, bất kể permission `DOCUMENT_DELETE`; (2) `restoreDocumentService` chỉ cho ADMIN HOẶC chính người tạo document — không phải mọi user có `DOCUMENT_UPDATE`. FE tự giới hạn hiển thị 2 action này tương ứng.
  - `GET /workflows/document/:documentId` throw 404 (không trả `null`) khi document chưa submit — hook `useDocumentWorkflow` phân biệt rõ "chưa submit" (404) với lỗi thật khác.
  - Quyết định kỹ thuật đáng chú ý: `DocumentMetaFields` (form nhập `meta`, dùng chung Create/Edit) phải dùng 1 Zod schema DUY NHẤT qua `.merge()` — 2 schema viết riêng dù giống cấu trúc bị TypeScript coi khác type khi phối hợp `useFieldArray`/`Path<T>` generic (lỗi build thật đã gặp và sửa).
  - "FE Workflow UI" (Duyệt/Từ chối/Huỷ/Hoàn tất) CHƯA làm — phạm vi riêng theo roadmap Mục 11, `DocumentDetailPage` chỉ hiển thị workflow READ-ONLY.
  - Mutation flows (create/update/delete/restore/submit) **NOT VERIFIED qua HTTP thật** (chủ động không tạo/sửa/xoá dữ liệu dev) — chỉ verify GET (read-only, bao gồm cả 2 case workflow 200/404) + review code kỹ so khớp DTO.
- **DEV-031 (2026-09-06) — Fix regression tự phát hiện**: route `/app/documents/:id` gate bằng `DOCUMENT_VIEW_DETAIL` (viết ở FE-04) đã bị vô hiệu hoàn toàn sau khi DEV-009A (backend, CÙNG NGÀY) gỡ permission này khỏi RBAC của 5 role thường — chặn CỨNG 100% non-admin khỏi xem BẤT KỲ document nào dù backend cho phép. Phát hiện khi chuẩn bị FE-05, đã sửa (bỏ guard tĩnh sai, giữ nguyên guard `DOCUMENT_VIEW` của nhóm cha, tin tưởng backend + `ErrorState` sẵn có xử lý 403). Xem `docs/development/tasks/DEV-031.md`.
- **FE-05 — FE Workflow UI (Duyệt/Từ chối/Huỷ/Hoàn tất): DONE (2026-09-06)**. Chi tiết: `docs/frontend/tasks/FE-05.md`. Tóm tắt:
  - Trang mới "Hộp thư chờ duyệt" (`/app/workflows/pending`, nav item mới) — `GET /workflows/pending` tự lọc theo role người gọi, FE chỉ permission-gate (không cần tính lại role/step matching như ở Document Detail).
  - `DocumentDetailPage` bổ sung 4 action Duyệt/Từ chối/Huỷ/Hoàn tất, ẩn/hiện theo ĐÚNG business rule đọc từ source (`workflow.service.ts`): Duyệt/Từ chối cần role khớp `steps[currentStep].role`; Huỷ/Hoàn tất chỉ người tạo document, đúng điều kiện trạng thái tương ứng.
  - 1 component dùng chung `WorkflowActionModal` (modal có textarea ghi chú, `commentRequired` chỉ là ràng buộc UX ở FE — backend luôn coi `comment` optional kể cả reject).
  - **HTTP verify THẬT đầy đủ luồng Submit→Pending list→Cancel** (tạo document test, submit, xuất hiện đúng ở pending list, cancel, xác nhận Document.workflowStatus đổi đúng, dọn dẹp toàn bộ dữ liệu test). **Approve/Reject/Complete CHƯA verify được bằng HTTP thật** — phát hiện quan trọng: role `USER` (duy nhất có tài khoản test) KHÔNG có RBAC `WORKFLOW_APPROVE`/`WORKFLOW_REJECT` (đúng thiết kế), và `admin` bị chặn bởi business rule role-match trong `approveStep`/`rejectStep` (role "ADMIN" không khớp step role "USER"/"IT") — cần tài khoản IT/TRUONG_KHOA/DIEU_DUONG_TRUONG/BAN_GIAM_DOC để verify đầy đủ.
  - **Phát hiện phụ (ngoài scope, ghi nhận không sửa)**: template mẫu có sẵn "Duyệt giấy tờ đề xuất 3 cấp" (USER→IT→ADMIN) có bước 1 role="USER" — nhưng role USER không có `WORKFLOW_APPROVE` nên KHÔNG AI có thể duyệt được bước này (không phải USER vì thiếu quyền, không phải ADMIN vì role-mismatch) — workflow dùng template này sẽ kẹt vĩnh viễn ở bước 1. Đây là vấn đề dữ liệu/business rule của backend, không phải bug FE.

## 2. Architecture decisions đã CHỐT

| Quyết định | Giá trị | Nguồn |
|---|---|---|
| Framework | React 18 + Vite + TypeScript | User xác nhận AskUserQuestion, trước UI-00 |
| Server state | TanStack React Query v5 | Implemented + verify FE-00/FE-01 |
| Client state (session+UI) | Zustand (`authStore`, `uiStore`, `toastStore`) | Implemented FE-00/FE-01 |
| Routing | React Router (v7, API kiểu v6) | Implemented FE-00/FE-01 |
| HTTP | Axios (instance + refresh-lock interceptor) | Implemented FE-00/FE-01 |
| Styling | Tailwind CSS v4 + shadcn/ui foundation (Radix primitives) | Implemented FE-01 — design token Modern Healthcare Enterprise (xanh dương/teal), KHÔNG còn neutral mặc định |
| Ant Design | CHƯA cài — chỉ dùng khi tới phase Dashboard (theo yêu cầu FE-01 gốc). **[CẬP NHẬT 2026-09-07]** Số thứ tự FE-XX đánh theo thứ tự THỰC TẾ triển khai (roadmap Mục 29), không cố định theo dự kiến gốc — RBAC Admin (không phải Dashboard) đã nhận số FE-08. | Deferred, chưa cần ở FE-00/01 |

## 3. Architecture decisions ĐỀ XUẤT (chưa chốt thêm ngoài Mục 2 — xem `FE_ARCHITECTURE.md`)

Type generation từ OpenAPI (`openapi-typescript`) — vẫn viết tay đến nay, chưa cần thiết ở quy mô hiện tại.

## 4. Completed phases

- **FE-00 — Frontend Project Bootstrap: DONE (2026-09-05)**.
- **FE-01 — Authentication + App Shell + UI Foundation: DONE (2026-09-05)** — xem Mục 1, chi tiết `docs/frontend/tasks/FE-01.md`.
- **FE-02 — RBAC / Permission UI Foundation: DONE (2026-09-05)** — xem Mục 1, chi tiết `docs/frontend/tasks/FE-02.md`.
- **FE-03 — Users / Departments UI thật: DONE (2026-09-05)** — xem Mục 1, chi tiết `docs/frontend/tasks/FE-03.md`.
- **FE-04 — Documents Core UI: DONE (2026-09-05)** — xem Mục 1, chi tiết `docs/frontend/tasks/FE-04.md`.
- **FE-05 — FE Workflow UI (Duyệt/Từ chối/Huỷ/Hoàn tất): DONE (2026-09-06)** — xem Mục 1, chi tiết `docs/frontend/tasks/FE-05.md`.
- **FE-06 — Assets Core UI: DONE increment đầu (2026-09-06)** — List/Detail/Create/Edit/Delete + Assign/Transfer/Return + Assignment History. Chi tiết `docs/frontend/tasks/FE-06.md`. Asset Categories CRUD/Excel-QR UI để lại phase sau. Cả 3 Remaining Issues ban đầu đã fix ở DEV-035/DEV-036.
- **FE-07 — Medical Devices UI: DONE (2026-09-07)** — Hồ sơ thiết bị y tế + lịch sử kiểm định, nhúng TRỰC TIẾP vào `AssetDetailPage` (KHÔNG route riêng — quyết định dựa trên thiết kế backend, mọi endpoint theo `:assetId`). Chi tiết `docs/frontend/tasks/FE-07.md`.
- **FE-08 — RBAC Admin UI: DONE (2026-09-07)** — CRUD đầy đủ Roles/Permissions/Policies + permission matrix (gán quyền cho role) — route mới `/app/rbac/{roles,roles/:id,permissions,policies}`. Chi tiết `docs/frontend/tasks/FE-08.md`.
- **FE-09 — Dashboard UI: DONE (2026-09-07)**, **nâng cấp UI (2026-09-08)**, **review bằng browser thật (2026-09-09)** — thay placeholder "Welcome" (FE-01) bằng dashboard nghiệp vụ đầy đủ cho 12 endpoint `dashboard.route.ts`, tại `/app` (index, KHÔNG route mới). Nâng cấp: layout Tabs (5 tab = 5 nhóm API, `@radix-ui/react-tabs` mới thêm), áp skill `dataviz` (icon+tone cho KPI card, tooltip hover thật, token màu mới `--chart-2`). **Review 2026-09-09**: LẦN ĐẦU verify được bằng browser thật (Playwright + Edge cài sẵn máy — hoá ra CÓ, trước đó tưởng không có công cụ) — sửa 6 vấn đề UI thật (cột dài bị cắt cứng, warranty thiếu badge "đã hết hạn", thiếu icon 1 tab, KPI tone dễ nhầm, trend chart 1-series khó đọc). **Phát hiện + ĐÃ SỬA 1 bug nghiêm trọng NGOÀI SCOPE, ẢNH HƯỞNG TOÀN APP** (xem Mục 6.20): `GET /users/me` không fire ngay sau login (60s đầu mọi permission check sai) — user xác nhận sửa ngay, đã fix + verify lại bằng Playwright. Chi tiết `docs/frontend/tasks/FE-09.md`.
- **FE-10 — Asset Categories UI: DONE (2026-09-09)** — hoàn thiện phần còn thiếu của FE-06 (CRUD đầy đủ cho `AssetCategory`, trước đó chỉ có list tối thiểu cho dropdown). Route mới `/app/assets/categories` (gate riêng `ASSET_CATEGORY_VIEW`), `AssetSectionTabs` liên kết 2 trang Tài sản/Danh mục. Cùng lúc fix gap giống DEV-035: `QueryAssetCategoryDTO` thêm `isActive` (trước hard-code `true`, không ai reach được danh mục đã xoá mềm để khôi phục) + sửa 1 lỗi tài liệu OpenAPI sẵn có (`AssetCategory.parentCategory` từng khai sai là ObjectId thô, thực tế luôn populate object). Không có UI "Xoá vĩnh viễn" — đúng tiền lệ `ASSET_DELETE_PERMANENT` (0 UI ở FE-06), permission tương ứng không gán cho role nào. Chi tiết `docs/frontend/tasks/FE-10.md`.
- **FE-11 — Audit Logs UI: DONE (2026-09-09)** — thay `PlaceholderPage` tại `/app/audit-logs` bằng trang thật: tab "Nhật ký" (list + filter ngày/hành động/người thực hiện/đối tượng + xuất Excel/CSV + drawer chi tiết) và tab "Thống kê" (`AUDIT_VIEW_DASHBOARD` — permission RIÊNG, không phải ai xem được list cũng xem được, vd role `IT`). Phát hiện quan trọng: response `GET /user-audits`/`GET /user-audits/dashboard` KHÔNG bọc `{message, data}` như mọi domain khác (đọc thẳng `response.data`, không qua `unwrapResponse()`). Không sửa backend. Chi tiết `docs/frontend/tasks/FE-11.md`.
- **FE-12 — Notifications UI: DONE (2026-09-10)** — hiện thực hoá chuông thông báo (`NotificationBell` ở `Header.tsx`, thay placeholder disabled từ FE-01) + trang đầy đủ `/app/notifications` (filter trạng thái đọc/loại, xoá, đánh dấu đã đọc). Self-scoped (không permission riêng). Phát hiện: response `GET /notifications` cũng KHÔNG theo shape `{data, pagination}` chuẩn (giống Audit Logs) — đọc thẳng `response.data.data`. `resourceType=WorkflowInstance`/`ImportHistory` KHÔNG build được deep-link (chưa có route đích ở FE) — chỉ `Document`/`Asset` bấm được. Chi tiết `docs/frontend/tasks/FE-12.md`.
- **FE-13 — Notifications: Quản trị cho ADMIN: DONE (2026-09-10)** — user hỏi "admin có cần quản trị notification không?" sau FE-12, đã đánh giá + đề xuất, user chọn làm cả 2: (A) `POST /notifications/broadcast` (permission MỚI `NOTIFICATION_BROADCAST`) — soạn gửi thông báo hệ thống tới tất cả/theo role/theo phòng ban/user cụ thể, tái dùng `notifyUserIds` có sẵn; (B) `GET /notifications/admin` (permission MỚI `NOTIFICATION_VIEW_ALL`) — xem thông báo của bất kỳ user nào (chỉ đọc, không xoá/đánh dấu thay). UI: tab "Quản trị" mới ở `/app/notifications`. Nhân tiện sửa 3 lỗi tài liệu OpenAPI có sẵn (shape pagination sai, field `count`→`unreadCount`, thiếu 1 giá trị enum). 97 test backend + 25 test frontend PASS, không regression. Chi tiết `docs/frontend/tasks/FE-13.md`.
- **FE-14 — Profile UI: DONE (2026-09-10)** — trang `/app/profile` (reach qua avatar menu ở `Header.tsx`, KHÔNG mục sidebar riêng — khác Notifications, Profile không có phần quản trị bên trong): thông tin tài khoản (đọc) + sửa họ tên/tên đăng nhập (đúng allowlist thật `updateMeService()`) + đổi mật khẩu (tự logout sau khi thành công vì backend revoke toàn bộ refresh token). **Phát hiện + ĐÃ FIX trước khi build**: 4 role `TRUONG_KHOA`/`DIEU_DUONG_TRUONG`/`BAN_GIAM_DOC`/`PHONG_VAT_TU_TTB` thiếu `USER_CHANGE_PASSWORD` (route tự-scope, không lý do gì loại trừ) — thêm vào `rolePermission.map.ts` + chạy lại `seed-rbac.ts`. Chi tiết `docs/frontend/tasks/FE-14.md`.
- **FE-15 — Upload / Excel UI: DONE (2026-09-10)** — "Quản lý file" độc lập (`/app/files`, thư viện file KHÔNG gắn Document/Asset nào) + Excel Import/Export cho Document (`DocumentsListPage` dropdown "Excel": xuất/nhập-preview-xác nhận/tải mẫu/lịch sử) + Asset (tương tự, tái dùng `ImportHistoryDrawer` chung) + Đồng bộ khoa/phòng từ Excel (`DepartmentsListPage`). **Bug backend ĐÃ FIX**: `fileUrl` trả về là đường dẫn chết (không có `express.static` phục vụ `uploads/`) — thêm route mới `GET /api/upload/:id/download` (có auth/ownership check, KHÔNG dùng `express.static` cho cả thư mục). Sửa 5 lỗi tài liệu OpenAPI có sẵn (2 ghi chú bảo mật lỗi thời, response thiếu `success`, schema thiếu field). Component mới dùng chung: `FileUpload`, `ExcelImportWizard` (generic), `utils/downloadBlob.ts`. Gap RBAC phát hiện (`PHONG_VAT_TU_TTB` thiếu `DOCUMENT_EXCEL_HISTORY`) CHƯA sửa — cần user quyết định hướng đặt tên permission. Chi tiết `docs/frontend/tasks/FE-15.md`.
- **DEV-038 — "Lịch sử duyệt" (Workflow History) + 2 nâng cấp Workflow: DONE (2026-09-10)** — user yêu cầu trực tiếp, ngoài roadmap. Tab "Lịch sử" mới ở trang "Duyệt tài liệu" (`/app/workflows/pending`, đổi nhãn sidebar từ "Chờ duyệt") — khác "Đang chờ" (chỉ hiện việc CẦN xử lý NGAY): hiển thị TOÀN BỘ workflow role người dùng từng/đang phải xử lý, filter theo trạng thái, ADMIN xem hết. Backend mới `GET /api/workflows/history`. Kèm review + **ĐÃ TRIỂN KHAI luôn 2 nâng cấp** (user yêu cầu ngay sau khi xem đề xuất): **(1) Gửi duyệt lại** — `DocumentDetailPage` giờ cho phép "Gửi duyệt lại" khi tài liệu đang `rejected`/`cancelled` (trước đây nút Submit biến mất vĩnh viễn dù backend đã hỗ trợ resubmit từ trước). **(2) ADMIN duyệt/từ chối thay** — `approveStep`/`rejectStep` thêm tham số `isAdmin`, cho phép ADMIN xử lý 1 bước dù role không khớp (trước đây NGAY CẢ ADMIN cũng bị 403, workflow có thể kẹt vĩnh viễn nếu cả role đó bị khoá/nghỉ hết) — comment tự gắn tiền tố `[ADMIN duyệt/từ chối thay role "X"]`, `approvedBy` vẫn lưu đúng người thật, FE hiện cảnh báo + đổi nhãn nút rõ ràng khi đang override. Chi tiết `docs/development/tasks/DEV-038.md`.
- **DEV-039 — Fix 403 "Xem chi tiết" từ tab "Lịch sử duyệt": DONE (2026-09-10)** — user báo lỗi thật kèm ảnh chụp NGAY SAU khi dùng tính năng DEV-038. Root cause: `GET /documents/:id` (Policy ABAC `document-view-detail-pending-approver`, DEV-034) VÀ `GET /documents/:proposalId/reports` (`getReportsByProposalService`, DEV-030) đều chỉ cho approver KHÁC phòng ban xem document khi workflow CÒN "pending" — hết pending (đã duyệt/từ chối/huỷ) thì mất quyền VĨNH VIỄN dù chính họ từng xử lý bước đó. Bug đã TỒN TẠI TỪ TRƯỚC (DEV-030/034) nhưng chưa ai chạm tới vì trước DEV-038 không có đường nào quay lại xem document sau khi workflow xong. **ĐÃ FIX**: Policy ABAC mới `document-view-detail-workflow-participant-role` (CỘNG THÊM, không sửa 2 Policy cũ) + ngoại lệ tương đương ở `getReportsByProposalService` — cả 2 dùng chung tiêu chí "role từng có mặt ở bất kỳ bước nào (mọi trạng thái) của bất kỳ WorkflowInstance nào gắn với document", khớp đúng cách `getWorkflowHistoryForUser` (DEV-038) đã định nghĩa "lịch sử". Nhân tiện đồng bộ `seed-rbac.ts` (Policy `pending-approver` của DEV-034 chưa từng được ghi vào script) + sửa 2 lỗi OpenAPI (permission `DOCUMENT_READ` sai tên → `DOCUMENT_VIEW`, 1 ghi chú lỗi thời khác). Chi tiết `docs/development/tasks/DEV-039.md`.
- **DEV-040 — Fix IT không xem được tài liệu khoa khác dù đã gán permission: DONE (2026-09-10)** — user báo lỗi trực tiếp ("đã chọn trong phân quyền rồi" nhưng trang "Tài liệu" vẫn rỗng). Root cause: department-scoping (DEV-030, `getAllDocumentsService`/`getReportsByProposalService`) trước đây CHỈ có 1 lối thoát — `isAdmin` — hoàn toàn KHÔNG đọc permission nào, nên bất kỳ permission nào user gán cho IT qua UI "Phân quyền" đều vô tác dụng với rule "chỉ xem cùng khoa". **ĐÃ FIX**: permission mới `DOCUMENT_VIEW_ALL_DEPARTMENTS` (opt-in, permission-driven, gán cho IT qua `rolePermission.map.ts`) làm lối thoát THỨ HAI song song `isAdmin` ở cả `getAllDocumentsService`/`getReportsByProposalService`; route `GET /documents/:id` nhận thêm permission này làm lựa chọn thay thế `DOCUMENT_VIEW_DETAIL` (tái dùng OR-permission có sẵn ở `authorizePermission.middleware.ts`, không cần Policy ABAC mới). CHỈ áp dụng hành động XEM (list/detail/reports) — cố tình không đụng update/delete/restore. Nhân tiện sửa drift OpenAPI (`GET /api/documents` từng ghi permission sai `DOCUMENT_READ` + mô tả 2 bug đã fix từ lâu nhưng còn ghi "đang mở"). Chi tiết `docs/development/tasks/DEV-040.md`.
- **DEV-041 — Review RBAC toàn diện (role/permission "chưa rõ ràng") + triển khai đủ 4 quyết định: DONE (2026-09-11)** — user yêu cầu trực tiếp sau chuỗi 5 lần vá liên tiếp cùng khu vực (DEV-030/034/038/039/040): phân tích rõ IT/Admin/User/các role khác. Phát hiện qua đọc DB thật: (1) **regression** — collection `policies` bị xoá sạch (0 document), đã fix; (2) tác dụng phụ khi fix (1): role `IT` mất 2 permission (`DOCUMENT_DELETE`/`DOCUMENT_VIEW_DETAIL`) đã gán tay qua UI "Phân quyền" nhưng KHÔNG có trong code — minh chứng sống cho root cause chính: "2 nguồn sự thật (code file vs DB qua UI) không đồng bộ, seed script ghi đè destructive"; (3) "xem theo khoa" cài đặt RỜI RẠC ở 4 nơi (list/detail/reports/update); (4) 7 role thực chất 2 nhóm khác bản chất (Vận hành vs Chỉ-để-duyệt) trình bày phẳng gây hiểu lầm; (5) `DIEU_DUONG_TRUONG` chưa dùng ở `WorkflowTemplate` nào. **User quyết định + ĐÃ TRIỂN KHAI cả 4**: (a) khôi phục 2 quyền cho IT (xác nhận có chủ đích, dù chồng chức năng với `DOCUMENT_VIEW_ALL_DEPARTMENTS`); (b) `DIEU_DUONG_TRUONG` giữ nguyên, sắp có kế hoạch dùng; (c) gộp 4 cơ chế department-scope thành 1 module dùng chung `documents.scope.ts` (`isSameDepartment`/`canViewAcrossDepartments`/`applyDepartmentFilter`, refactor có kiểm soát, KHÔNG đổi hành vi, 12 test mới); (d) `seed-rbac.ts` đổi chính sách nguồn-sự-thật — `seedRoles()` (ghi đè Role.permissions) giờ CHỈ chạy khi có cờ `--sync-roles` tường minh, mặc định chỉ đồng bộ Permission/Policy (additive, an toàn) — DB/UI "Phân quyền" chính thức là nguồn sự thật. 135/135 test PASS (123+12), build sạch. Chi tiết `docs/development/tasks/DEV-041.md`.
- **DEV-043 — Dashboard "Tổng quan" của IT toàn số 0: DONE (2026-09-12)** — user báo lỗi kèm ảnh (Network tab + màn Dashboard toàn 0, dù đã tick `DASHBOARD_READ`). Root cause xác nhận qua query DB trực tiếp: **KHÔNG phải bug** — IT thuộc "Phòng Công nghệ thông tin", phòng này THẬT SỰ chưa từng có Document/Đề xuất/Báo cáo nào (0/0/0/1 user, khớp CHÍNH XÁC ảnh chụp) vì đây là bộ phận hỗ trợ kỹ thuật, không tạo đề xuất/báo cáo nghiệp vụ như khoa lâm sàng. `DashboardPage.tsx` (`isAdmin ? AdminSummarySection : DepartmentSummarySection(own dept)`) khoá non-admin vào ĐÚNG khoa mình — không liên quan gì permission `DASHBOARD_READ` (chỉ quyết định có vào được trang không). Phát hiện phụ: `GET /dashboard/department/:id` (backend) từ trước KHÔNG hề giới hạn `departmentId` theo khoa người gọi — bất kỳ role có `DASHBOARD_READ` đã CÓ THỂ gọi thẳng API cho khoa bất kỳ, chỉ UI ẩn nút chọn. User chọn hướng: mở nút "Xem theo khoa/phòng khác" (trước CHỈ ADMIN) cho bất kỳ ai có permission `DOCUMENT_VIEW_ALL_DEPARTMENTS` (DEV-040, hiện IT) — permission-driven, không hardcode role, KHÔNG cần sửa backend (API vốn đã không giới hạn). **Follow-up cùng ngày**: user xác nhận qua ảnh thật rồi yêu cầu ẩn luôn block "Tổng quan" mặc định theo khoa CHÍNH IT (luôn `0/0/0/1`, dư thừa khi đã có phần chọn khoa) — nhóm user có `canDrilldownOtherDepartments` (không phải ADMIN) giờ CHỈ còn thấy phần chọn khoa/phòng, không còn block mặc định; role khác không có permission này giữ nguyên hành vi cũ. 25/25 test frontend PASS cả 2 lần. Chi tiết `docs/development/tasks/DEV-043.md`.
- **DEV-044→048 — "Backend Open Risks" (5 việc user yêu cầu xử lý lần lượt theo thứ tự ưu tiên): DONE (2026-09-12)** — sau khi rà soát roadmap (`docs/30_DEVELOPMENT_COMPLETION_AUDIT.md`), user chọn xử lý cả 5 vấn đề còn mở từ 25 task gốc:
  - **DEV-044**: `DELETE /documents/delete-by-month` thiếu guard ADMIN-only (khác xoá đơn lẻ đã có từ DEV-006) — càng cấp thiết sau khi IT được cấp lại `DOCUMENT_DELETE` (DEV-041). Thêm guard mirror đúng `deleteDocumentService`.
  - **DEV-045**: TOCTOU (RV05-07) dò trùng đề xuất sửa chữa Asset — cùng họ lỗi ARCH-21 (đã fix ở Excel import) nhưng chưa áp dụng cho domain Asset. Chuyển check vào trong transaction, dùng session.
  - **DEV-046**: `syncAssetOnDocumentApproved` (fix CRITICAL nhất dự án, DEV-005) có 0% test coverage — bổ sung 7 test (qua `approveStep`, không export hàm nội bộ chỉ để test).
  - **DEV-047**: DEV-001A Phase B — user xác nhận dự án chỉ có 1 môi trường (không có production riêng) → set `isSystemRole:true` cho ADMIN thật + gỡ HOÀN TOÀN lưới đỡ literal `role.name==="ADMIN"` khỏi ~20 vị trí, đóng dứt điểm RV02-01 (role rename hijack).
  - **DEV-048**: dựng khung E2E test ĐẦU TIÊN của dự án (supertest + `mongodb-memory-server`, tách hẳn khỏi unit suite) + 10 test cho luồng CRITICAL nhất — bao gồm toàn bộ chuỗi Document→Workflow→sync Asset (DEV-005) lần đầu được xác nhận bằng HTTP thật.
  
  Tổng: 150→160 test backend PASS (thêm 10 unit + 10 E2E riêng), build sạch, không regression. Chi tiết từng việc: `docs/development/tasks/DEV-044.md` → `DEV-048.md`.
- **FE-16 — Global UI Polish: DONE (2026-09-12)** — audit có hệ thống toàn `frontend/src` theo `FE_UI_DEVELOPMENT_ROADMAP.md` Mục 32 (status color/table density/modal-drawer/empty-error-loading state/debounce/icon-button a11y/responsive width/`<img alt>`) — **0 vi phạm mới** tìm thấy (FE-01→FE-15 đã giữ chuẩn nhất quán qua shared components sẵn có). Finding thật duy nhất: **FE/BE contract drift do chính DEV-047** (task ngay trước) gây ra — DEV-047 gỡ dual-check `isSystemRole || role.name==="ADMIN"` ở BACKEND nhưng bỏ sót 6 vị trí FE vẫn giữ dual-check cũ (`useIsAdmin.ts`, `roleGuards.ts#isProtectedSystemRole`, `useRoles.ts#useAssignableRoles`, + 4 page tự lặp lại inline). Đã đồng bộ toàn bộ 6 nơi về CHỈ `isSystemRole===true`, đồng thời gộp 4 chỗ tự tính `isAdmin` inline về dùng chung `useIsAdmin()` (giảm trùng lặp, CLAUDE.md Mục 11). Test: `typecheck`/`build`/`oxlint` PASS, vitest **25/25 PASS** (không đổi). Chi tiết: `docs/frontend/tasks/FE-16.md`.
- **DEV-049 — Tìm kiếm/lọc cho module File Upload (Backend + Frontend): DONE (2026-09-12)** — theo yêu cầu trực tiếp của user. Backend: `QueryUploadDTO` mới (keyword/mimeType/uploadedBy/fromDate/toDate/sortBy/order, cùng pattern `QueryDocumentDTO`) + RÚT logic `getFiles()` từ thẳng trong controller sang `upload.service.ts` (`buildFilesFilter`/`getFilesService`, sửa luôn bất nhất kiến trúc — domain Upload trước đây là NGOẠI LỆ DUY NHẤT không theo flow Controller→Service→Model) + OpenAPI cập nhật. Frontend: `FilesListPage.tsx` thêm `FilterBar` (tìm tên file debounce, lọc loại file/khoảng ngày/người tải lên — dropdown người tải lên CHỈ hiện cho ADMIN qua `useIsAdmin()`) + wire sort theo cột (trước đây trang DUY NHẤT dùng `DataTable` mà chưa bật sort). Test: backend unit 150→160 (thêm 10, domain Upload lần đầu có test), frontend 25/25, build/typecheck/lint sạch cả 2 phía. Chi tiết: `docs/development/tasks/DEV-049.md`.
- **DEV-050 — Feature Roadmap A1: QR Code cho Asset (UI): DONE (2026-09-14)** — user chọn mục A1 trong `docs/development/FEATURE_DEVELOPMENT_ROADMAP.md`. **Phát hiện quan trọng**: backend ĐÃ CÓ SẴN TOÀN BỘ từ trước (`assetQRCode.service.ts` — sinh QR/lookup/check-in, đã có route+permission `ASSET_INVENTORY_CHECK`+OpenAPI đầy đủ, QR chủ đích encode `assetCode` chứ không phải URL/`assetId`) — task thực tế CHỈ còn phần Frontend. Thêm: `AssetQRCodeSection.tsx` (hiển thị/tải/in QR ở `AssetDetailPage`, phải tự tạo Object URL từ Blob vì app dùng Bearer header không phải cookie — không thể `<img src="/api/...">` trực tiếp) + `AssetScanPage.tsx` (`/app/assets/scan` — nhập/quét mã → tra cứu → xác nhận kiểm kê, CHỦ Ý KHÔNG thêm thư viện camera-scan mới, dùng đúng thiết kế backend "đầu đọc quét = bàn phím ảo"). Test: backend không đổi (160/160 PASS), frontend typecheck/test(25/25)/lint(0 warning mới, đã sửa 1 warning `set-state-in-effect` phát sinh khi code)/build đều sạch. Chi tiết: `docs/development/tasks/DEV-050.md`.

## 5. Completed features

- **Users**: CRUD (trừ xoá vĩnh viễn — chỉ soft-delete/disable), gán role, đặt lại mật khẩu, khôi phục — permission-aware, đầy đủ (FE-03).
- **Departments**: CRUD đầy đủ (kể cả xoá — hard delete, backend tự chặn nếu còn tham chiếu) — permission-aware (FE-03).
- **Documents**: List (filter/sort đầy đủ)/Create (Proposal, 3 subType)/Detail (view/edit/xoá/khôi phục)/xoá theo tháng — permission-aware + 2 rule ẩn ADMIN-gated ở UI (xoá, khôi phục) — hiển thị workflow trạng thái + submit vào workflow (FE-04) + đầy đủ 4 action Duyệt/Từ chối/Huỷ/Hoàn tất (FE-05).
- **Workflow**: hộp thư chờ duyệt (`/app/workflows/pending`) + action Duyệt/Từ chối/Huỷ/Hoàn tất permission-aware + state-aware (FE-05).
- **Assets**: List (filter/sort/`isActive`)/Create/Detail (view/edit/xoá/khôi phục) + Cấp phát/Luân chuyển/Thu hồi + lịch sử cấp phát, điều kiện hiển thị action khớp đúng `assetAssignment.service.ts` (FE-06 + DEV-035/036). **Asset Categories**: CRUD đầy đủ (kể cả xoá mềm/khôi phục, trừ xoá vĩnh viễn) tại `/app/assets/categories` (FE-10). Excel import (chỉ phục vụ Document, không áp dụng Assets) và QR code (theo từng Asset, không phải theo Category) VẪN CHƯA có UI.
- **Medical Devices**: hồ sơ thiết bị y tế (phân loại pháp lý, giấy phép, chu kỳ kiểm định) + lịch sử kiểm định, nhúng trong Asset Detail (FE-07). Upload chứng nhận file thật CHƯA có UI (chỉ nhận link).
- **RBAC Admin**: CRUD Roles (kèm permission matrix gán quyền)/Permissions/Policies (ABAC) — permission-aware, đã verify ABAC hoạt động runtime thật (FE-08).
- **Dashboard**: 12 widget đầy đủ (Admin/Department Summary, Asset Summary, Medical Device Summary, KPI trends, 3 alert list) tại `/app` — role `USER` (duy nhất thiếu `DASHBOARD_READ`) thấy Welcome panel tối giản thay thế (FE-09).
- **Audit Logs**: List + filter (ngày/hành động/người thực hiện/đối tượng) + xuất Excel/CSV + drawer chi tiết + tab Thống kê (permission riêng `AUDIT_VIEW_DASHBOARD`) tại `/app/audit-logs` (FE-11).
- **Notifications**: chuông thông báo (`Header.tsx`, badge số chưa đọc poll 30s) + trang đầy đủ `/app/notifications` (có mục sidebar riêng, không gán permission — self-scoped) — filter đọc/chưa đọc + loại, đánh dấu đã đọc, xoá (FE-12). **Quản trị ADMIN** (FE-13): tab "Quản trị" trong CÙNG trang — gửi thông báo hệ thống thủ công (tất cả/theo role/theo phòng ban/user cụ thể) + xem thông báo mọi user (giám sát/debug, chỉ đọc) — 2 permission riêng `NOTIFICATION_BROADCAST`/`NOTIFICATION_VIEW_ALL`. Mục sidebar ban đầu KHÔNG có (chỉ reach qua chuông), đã thêm lại sau khi user phản hồi thiếu, xem `FE-13.md` Mục 4b.
- **Profile**: trang `/app/profile` (reach qua avatar menu, không mục sidebar) — thông tin tài khoản (đọc, gồm badge vai trò/phòng ban — KHÔNG dump permission kỹ thuật) + sửa họ tên/tên đăng nhập + đổi mật khẩu (tự logout sau thành công) (FE-14).
- **Files/Excel**: "Tệp tin" (`/app/files`) — thư viện file upload ĐỘC LẬP (không gắn Document/Asset). Excel: Export/Import (preview dry-run trước khi ghi thật)/Tải mẫu/Lịch sử import cho Document (`DocumentsListPage`) và Asset (`AssetsListPage`, import chỉ tạo mới) qua dropdown "Excel"; Đồng bộ khoa/phòng từ Excel ở `DepartmentsListPage` (FE-15).
- FE-00/FE-01/FE-02 CHỈ là foundation/infrastructure (auth, RBAC context, App Shell, design system) — Users/Departments (FE-03) là tính năng nghiệp vụ THẬT đầu tiên, Documents (FE-04) là domain trung tâm nhất.

## 6. Known issues / Frontend-Backend findings (đầy đủ ở từng file, tóm tắt tại đây)

1. Response shape KHÔNG đồng nhất 100% giữa domain (xem `ERROR_HANDLING.md`, `API_REFERENCE.md`) — cần lớp `unwrapResponse`/`parseApiError` xử lý nhiều shape.
2. ~~`GET /api/users`/`GET /api/users/:id` hiện 403 với MỌI non-ADMIN~~ — **ĐÃ QUYẾT ĐỊNH (DEV-027, 2026-09-05)**: role `IT` được cấp thêm `USER_VIEW`+`USER_VIEW_DETAIL` (chỉ xem), các role khác vẫn không có. Không còn pending.
3. Domain Upload: 2 nhánh lỗi (404/403) không qua `error.middleware.ts` chuẩn, shape lỗi khác biệt (chỉ `{message}`, không `success`/`errorCode`).
4. Dashboard: 12 endpoint KHÔNG có `validateQuery`/Zod DTO — FE phải tự validate `month`/`year`/`daysAhead`/`daysThreshold` phía client, backend có thể trả kết quả sai/rỗng âm thầm thay vì 400.
5. `refreshToken` trả trong JSON body (không phải httpOnly cookie) — buộc FE lưu `localStorage`, tăng bề mặt XSS so với thiết kế cookie lý tưởng (không tự sửa backend).
6. `DELETE /documents/delete-by-month` thiếu ADMIN-only guard ở backend (role `IT` cũng có `DOCUMENT_DELETE`) — `FRONTEND_RECOMMENDATION`: tự giới hạn hiển thị UI cho ADMIN dù backend chưa chặn.
7. ~~ABAC (Policy CRUD) hoạt động ở tầng lưu trữ nhưng 0 route nào áp dụng runtime (`enablePolicies` chưa bật) — cân nhắc gắn nhãn "Beta/chưa áp dụng" ở UI quản trị Policy~~ — **OUTDATED, ĐÃ XÁC MINH SAI (DEV-034, 2026-09-06 + FE-08, 2026-09-07)**: `document.route.ts` ĐÃ bật `enablePolicies` cho `GET /documents/:id`, và DEV-034 đã tạo + verify 1 Policy thật hoạt động đúng qua HTTP (200 khi điều kiện khớp, 403 khi không). ABAC ĐANG hoạt động runtime — `PoliciesListPage` (FE-08) KHÔNG gắn nhãn Beta.
8. `CreateRoleDTO`/`UpdateRoleDTO` KHÔNG có field `permissions` (cố ý, an ninh) — form Tạo Role và form Gán quyền PHẢI là 2 bước riêng.
9. ~~`validateQuery` middleware crash 500 trên MỌI route có query pagination/filter~~ — **ĐÃ FIX (DEV-027, 2026-09-05)**: bug Express 5 (`req.query` là accessor getter-only, gán `=` trực tiếp throw TypeError). Đã verify lại `GET /users`/`GET /documents`/`GET /rbac/roles` đều 200 sau fix. Không còn ảnh hưởng FE — có thể build DataTable/FilterBar dựa vào các endpoint list bình thường.
10. **`deleteDocumentService`/`restoreDocumentService` có rule ẩn khắt khe hơn permission catalog** (CONFIRMED, FE-04): xoá document CHỈ ADMIN (bất kể `DOCUMENT_DELETE`); khôi phục CHỈ ADMIN hoặc chính người tạo (bất kể `DOCUMENT_UPDATE`). FE đã tự giới hạn hiển thị 2 action này ở UI khớp đúng rule thật, nhưng `DOCUMENT_DOMAIN_MAP.md` Mục 2 (viết trước FE-04) chưa ghi 2 rule này — nên cập nhật tài liệu đó ở lần sửa tiếp theo (ngoài phạm vi FE-04, chỉ ghi nhận ở đây).
11. ~~Không có endpoint liệt kê `WorkflowTemplate`~~ — **ĐÃ FIX (DEV-028, 2026-09-05)**: bổ sung `GET /workflows/templates` (đọc-only, permission `WORKFLOW_VIEW`). FE-04 dùng cho `SubmitWorkflowModal`.
12. **[MỚI, FE-05]** Route `/documents/:id` từng gate bằng `DOCUMENT_VIEW_DETAIL` — **ĐÃ FIX (DEV-031, 2026-09-06)**: permission đó bị DEV-009A gỡ khỏi RBAC 5 role thường (chỉ còn ABAC per-resource), route guard tĩnh cũ chặn nhầm 100% non-admin. Đã bỏ guard thừa, dựa vào backend + `ErrorState`.
13. ~~Template mẫu "Duyệt giấy tờ đề xuất 3 cấp" (bước 1 role="USER") không ai duyệt được~~ — **ĐÃ FIX (DEV-032, 2026-09-06)**: sửa cả 3 bước khớp đúng thiết kế tổ chức đã ghi trong `rolePermission.map.ts` — TRUONG_KHOA (bước đầu) → PHONG_VAT_TU_TTB (bước giữa, thẩm định kỹ thuật) → BAN_GIAM_DOC (bước cuối). Data-only fix, không đổi RBAC/code. Xem `docs/development/tasks/DEV-032.md`.
14. **[MỚI, DEV-033]** Dropdown "Khoa/Phòng" ở `DocumentCreatePage` (FE-04) LUÔN TRỐNG với mọi tài khoản role `USER` — `GET /departments` yêu cầu `DEPARTMENT_VIEW`, chỉ `IT` có (trong 2 role có `DOCUMENT_CREATE`). **ĐÃ FIX (2026-09-06)**: khoá field về đúng khoa người tạo (đọc từ `user.department`, không cần API) khi thiếu quyền — không cấp thêm RBAC. Bug có từ FE-04, phát hiện qua user báo lỗi thật. Xem `docs/development/tasks/DEV-033.md`.
15. **[MỚI, DEV-034]** Role `PHONG_VAT_TU_TTB` THIẾU HẲN `DOCUMENT_VIEW` từ lúc tạo — chặn cứng route `/app/documents/*` (redirect `/403` ngay ở FE) VÀ `GET /documents` (list). Kể cả có `DOCUMENT_VIEW`, `GET /documents/:id` vẫn chặn document liên phòng ban đang chờ CHÍNH role này duyệt (rủi ro đã cảnh báo trước ở DEV-009A). **ĐÃ FIX (2026-09-06)**: thêm `DOCUMENT_VIEW`; Policy ABAC mới `document-view-detail-pending-approver` (`resource.pendingApproverRole===user.role.name`) cho xem chi tiết document liên phòng ban khi ĐANG chờ đúng role đó duyệt. User báo lỗi thật kèm screenshot. Xem `docs/development/tasks/DEV-034.md`.
16. ~~Role `PHONG_VAT_TU_TTB` có `ASSET_CREATE`/`ASSET_ASSIGN` NHƯNG KHÔNG có `DEPARTMENT_VIEW`/`USER_VIEW`~~ — **ĐÃ FIX (DEV-036, 2026-09-07)**: cấp thẳng 2 quyền này cho role (đối chiếu bằng chứng với `IT`, cùng nhu cầu liên phòng ban) — không cần sửa code FE, dropdown tự hiện đầy đủ. Xem `docs/development/tasks/DEV-036.md`.
17. ~~`GET /assets` HARD-CODE `filter.isActive=true`, không có cách khôi phục qua UI~~ — **ĐÃ FIX (DEV-035, 2026-09-07)**: thêm `isActive` vào `QueryAssetDTO`/`getAllAssetsService` (cùng pattern Document) + filter "Hiển thị"/action Khôi phục ở `AssetsListPage`. `GET /assets/:id` CỐ TÌNH giữ nguyên (parity với Document — detail vẫn luôn 404 với record đã xoá ở cả 2 domain). Xem `docs/development/tasks/DEV-035.md`.
18. **[MỚI, FE-08]** Role ADMIN thật ở DB dev có `isSystemRole: false` (xác nhận qua HTTP thật, tài khoản `admin`) — bypass Super-Admin ở `authorizePermission.middleware.ts` HIỆN CHỈ hoạt động qua nhánh OR `role.name === "ADMIN"` (migration DEV-001A "Phase A" chưa chạy xong ở environment này, xem comment gốc trong middleware). Bất kỳ UI/logic nào tự bảo vệ "role hệ thống" PHẢI check CẢ 2 điều kiện (`isSystemRole===true || name==="ADMIN"`, xem `useRoles.ts` `useAssignableRoles()` — pattern đã có từ FE-03 — và `features/rbac/utils/roleGuards.ts` `isProtectedSystemRole()` mới thêm ở FE-08), KHÔNG được chỉ check `isSystemRole`.
19. **[MỚI, FE-09]** `GET /dashboard/department/:departmentId` KHÔNG kiểm tra `departmentId` có khớp khoa của người gọi hay không — bất kỳ user có `DASHBOARD_READ` đều xem được dashboard của khoa BẤT KỲ qua URL trực tiếp (đọc trực tiếp `getDepartmentDashboard()` controller, không có check nào ngoài permission). Rủi ro THẤP (số liệu tổng hợp, không phải bản ghi chi tiết nhạy cảm) nhưng là khoảng trống ABAC — NGOÀI SCOPE FE-09, không tự sửa backend. FE giảm thiểu ở tầng UX: non-ADMIN chỉ tự xem khoa của chính mình (không có dropdown), ADMIN có dropdown chọn khoa (đằng nào cũng bypass mọi permission). Xem `docs/frontend/tasks/FE-09.md`.
20. ~~`GET /users/me` KHÔNG fire ngay sau login thành công~~ — **ĐÃ FIX (2026-09-09)**. Phát hiện khi review Dashboard bằng browser thật (Playwright), ẢNH HƯỞNG TOÀN APP không riêng Dashboard: `useLogin.ts` seed cache `["users","me"]` bằng `LoginUser` (subset rút gọn, THIẾU `permissions[]`) qua `setQueryData`, trong khi `useCurrentUser.ts` có `staleTime:60_000` (KHÔNG phải `0` như comment gốc từng mô tả sai) — `setQueryData` không tự đánh dấu stale nên KHÔNG có refetch nào xảy ra trong 60 giây đầu sau login, mọi permission check trả `false` sai (sidebar rỗng, dashboard fallback, mọi trang permission-gated khác về lý thuyết cũng bị ảnh hưởng). Đã sửa: `useLogin.ts` thêm `queryClient.invalidateQueries({queryKey:["users","me"]})` ngay sau `setQueryData` — verify lại bằng Playwright xác nhận `GET /users/me` giờ gọi đúng 1 lần (200) ngay sau login (không cần F5), sidebar hiện đủ nav item. Xem `docs/frontend/tasks/FE-09.md` Mục "Phát hiện quan trọng ngoài scope".

21. **[MỚI, FE-10]** `GET /assets/asset-categories` HARD-CODE `filter.isActive=true`, không có cách khôi phục qua UI — **ĐÃ FIX (2026-09-09)**: cùng nguyên nhân + cùng cách sửa DEV-035 (Assets), thêm `isActive` vào `QueryAssetCategoryDTO`/`getAllAssetCategoriesService` + filter "Hiển thị"/action Khôi phục ở `AssetCategoriesListPage`. Nhân tiện sửa 1 lỗi tài liệu OpenAPI sẵn có (không liên quan fix này): `AssetCategory.parentCategory` từng khai sai kiểu `ObjectId` thô, thực tế BE luôn populate object `{_id,code,name}` ở response GET. Xem `docs/frontend/tasks/FE-10.md`.

22. **[MỚI, FE-13, QUAN TRỌNG — pattern sẽ lặp lại với MỌI permission mới]** Thêm 1 permission mới vào `permission.constant.ts` + `rolePermission.map.ts` KHÔNG tự cấp quyền thật cho user nào — `GET /users/me` (nguồn của `usePermission().hasPermission()`) tính qua `getUserEffectivePermissions()` (`permission.service.ts`), hàm này đọc `role.permissions` — mảng ObjectId trỏ `Permission` THẬT trong MongoDB, không đọc `rolePermission.map.ts` trực tiếp. `rolePermission.map.ts` chỉ là nguồn cho `scripts/seed-rbac.ts`. Phát hiện khi user báo không thấy nút "Gửi thông báo hệ thống"/tab "Quản trị" dù đã có `NOTIFICATION_BROADCAST`/`NOTIFICATION_VIEW_ALL` trong code — DB chưa có 2 Permission này. **Lần đầu: KHÔNG tự chạy `seed-rbac.ts`** (SET LẠI permissions của MỌI role theo code, rủi ro ghi đè role đã chỉnh tay khác code, xem tình huống tương tự ở #15 DEV-034) — hướng dẫn user tự tạo Permission + gán vào role qua RBAC Admin UI (FE-08); **user yêu cầu trực tiếp chạy script** ngay sau đó (đã verify `MONGO_URI` local trước khi chạy) — xem `docs/frontend/tasks/FE-13.md` Mục 4c. **Quy tắc cho MỌI task sau này thêm permission mới**: phải nhắc user (hoặc tự làm nếu được yêu cầu rõ) bước tạo Permission + gán role trong DB, không chỉ dừng ở sửa code.
23. **[MỚI, FE-14]** Route `PATCH /users/change-password` gate `authorizePermission("USER_CHANGE_PASSWORD")`, nhưng permission này chỉ gán cho role `IT`/`USER` — 4 role "Giai đoạn 3" (`TRUONG_KHOA`/`DIEU_DUONG_TRUONG`/`BAN_GIAM_DOC`/`PHONG_VAT_TU_TTB`) bị 403 khi tự đổi mật khẩu dù hành động này tự-scope tuyệt đối (`req.user!._id`, không có tham số user khác). **ĐÃ FIX (2026-09-10)**: thêm permission cho cả 4 role trong `rolePermission.map.ts` + `npx ts-node scripts/seed-rbac.ts`. Xem `docs/frontend/tasks/FE-14.md`.
24. ~~`PATCH /users/me` (và `PUT /users/:id`, `POST /users`) khi trùng `username` ném `MongoServerError` E11000 → 500 generic thay vì lỗi rõ nghĩa~~ — **ĐÃ FIX (2026-09-10, user yêu cầu trực tiếp)**. Verify lại kỹ hơn nhận định ban đầu (SAI 1 phần): `create()`/`update()` (`POST /users`/`PUT /users/:id`) ĐÃ CÓ SẴN pre-check `findOne`+`ApiError.conflict` từ trước — CHỈ `updateMeService()` (`PATCH /users/me`, dùng bởi Profile FE-14) là THIẾU. Sửa 2 lớp: (a) thêm pre-check giống hệt `update()` vào `updateMeService()` (`users.service.ts`); (b) thêm nhánh lưới đỡ TOÀN APP trong `error.middleware.ts` nhận diện `err.code===11000` → 409 Conflict + message kèm tên field/giá trị trùng (map nhãn cho `username/email/code/name/documentCode/assetCode` — mọi field `unique:true` tìm được qua grep models), phòng race-condition và các chỗ khác lỡ thiếu pre-check tương tự. Thêm 6 test mới (`error.middleware.test.ts` +3, `users.service.test.ts` +3). Cập nhật OpenAPI: thêm `409` cho `PATCH /users/me` + `PUT /users/{id}` (thiếu tài liệu dù `PUT` đã trả 409 từ trước — drift có sẵn, sửa nhân tiện). Backend 103/103 test PASS (97 cũ + 6 mới).
25. **[MỚI, FE-15]** `fileUrl` (`/uploads/<filename>`, trả về từ mọi endpoint domain Upload — CẢ Calibration Record certificate cũng dùng chung pattern này) là đường dẫn CHẾT — không có `express.static` nào phục vụ thư mục `uploads/`. **ĐÃ FIX cho domain Upload** (2026-09-10): thêm `GET /api/upload/:id/download` (có auth/ownership check, KHÔNG dùng `express.static` cho cả thư mục — tránh lộ file người khác). Calibration Record (`certificateFileUrl`, dùng ở FE-07 `MedicalDeviceProfileModal`) VẪN CÒN bug tương tự — NGOÀI SCOPE FE-15, chỉ ghi nhận, chưa sửa.
26. ~~Role `PHONG_VAT_TU_TTB` có `ASSET_EXCEL_IMPORT` nhưng KHÔNG có `DOCUMENT_EXCEL_HISTORY`~~ — **ĐÃ FIX (2026-09-10, user yêu cầu trực tiếp)**: gán thêm `PERMISSIONS.DOCUMENT_EXCEL_HISTORY` cho role trong `rolePermission.map.ts` + chạy lại `npx ts-node scripts/seed-rbac.ts` (verify: role 28→29 permission, các role khác không đổi). Quyết định KHÔNG đổi tên permission (dù tên gợi ý "Document" trong khi endpoint dùng chung 2 domain) — theo đúng tiền lệ đã ghi sẵn trong chính `rolePermission.map.ts` ("đổi tên đã-deploy rủi ro hơn thêm quyền mới") + mô tả seed sẵn có ("Xem lịch sử nhập Excel") vốn đã trung lập. An toàn vì `getImportHistory` luôn lọc `importedBy: req.user._id` cho non-ADMIN. Đối chiếu toàn bộ `rolePermission.map.ts`: chỉ `IT`/`PHONG_VAT_TU_TTB` có bất kỳ quyền `ASSET_EXCEL_*` nào, `IT` đã có sẵn `DOCUMENT_EXCEL_HISTORY` — xác nhận không còn role nào khác thiếu quyền tương tự. Xem `docs/frontend/tasks/FE-15.md` Mục 1/3.2.
27. **[MỚI, FE-15, phát hiện SAU khi user thử file thật, 2026-09-10]** `workbook.getWorksheet(1)` (exceljs) tra theo ID SHEET GỐC lưu trong file .xlsx, KHÔNG theo vị trí tab — file thật do Excel/LibreOffice/WPS lưu qua nhiều lần xoá/tạo sheet thường có ID sheet đầu ≠ 1, gây lỗi 400 "Không tìm thấy sheet" SAI dù file hợp lệ, có dữ liệu ở tab đầu tiên (user gặp khi "Xem trước" import Asset với file `BVHC tháng 6.xlsx`). Lỗi có ở 3 hàm dùng chung pattern: `importAssetsExcel()` (`assetExcel.service.ts`), `importDocumentsExcel()`, `syncDepartmentFromExcel()` (`excel.service.ts`). **ĐÃ FIX cả 3** (2026-09-10): đổi sang `workbook.worksheets[0]` (getter trả theo vị trí tab thật, đúng ngữ nghĩa). Test hiện có không bắt được vì tự tạo workbook trong session (luôn id=1) — không tái hiện được file đã qua chỉnh sửa nhiều lần. Xem `docs/frontend/tasks/FE-15.md` Mục 3.1.

## 7. Pending decisions (cần hỏi user trước khi code)

- Phạm vi MVP cụ thể (bắt đầu từ domain nào) — CHƯA CHỐT, đã hỏi 2 lần.
- ~~Styling foundation~~ — **ĐÃ CHỐT tại FE-01**: Tailwind v4 + shadcn/ui (Radix), design token Modern Healthcare Enterprise. Có thể tinh chỉnh màu/spacing cụ thể sau nếu có brand guideline thật từ hospital, nhưng cấu trúc token không đổi.
- `/register` có nên public hay chỉ ADMIN tạo user qua `/users` — CHƯA CHỐT (route `/register` CHƯA implement ở FE-01, đúng scope — chỉ `/login` được yêu cầu).
- Quyết định nghiệp vụ cho `USER_VIEW`/`USER_VIEW_DETAIL` (Mục 6.2) — CHƯA CHỐT, ảnh hưởng trực tiếp trang `/app/users` (hiện chỉ placeholder).
- ~~`GET /users/me` không trả effective permission list~~ — **ĐÃ FIX** (DEV-026) + **`usePermission.ts` ĐÃ CẬP NHẬT** (FE-01) để dùng `user.permissions` thật. Không còn pending.

## 8. Technical debt (kế thừa từ backend, ảnh hưởng FE)

Xem đầy đủ `docs/30_DEVELOPMENT_COMPLETION_AUDIT.md` Mục 8-9. Đáng chú ý nhất với FE: 0 HTTP/E2E test nào tồn tại ở backend cho tới nay — FE không có "API sandbox đã verify qua request thật" để đối chiếu, mọi hành vi API trong bộ tài liệu này đến từ đọc source code + OpenAPI, KHÔNG phải từ gọi thử API thật.

## 9. Important conventions (bắt buộc tuân thủ khi code FE)

- Copy permission string TRỰC TIẾP từ `permission.constant.ts` — không tự đặt tên.
- Copy enum (`DocumentCategory`, `DocumentSubType`, `AssetStatus`, `workflowStatus` values) TRỰC TIẾP từ model backend — không tự thêm/bớt giá trị.
- Mọi field pagination dùng tên `totalPages` (đã thống nhất toàn hệ thống, DEV-025) — KHÔNG dùng `totalPage`.
- FE permission check CHỈ là UX guard — backend luôn là ranh giới bảo mật cuối cùng.
- **[MỚI, 2026-09-09] Verify UI bằng browser thật LÀ KHẢ THI trên máy user** — nhiều task trước (FE-06→FE-09 gốc) đều ghi "không có công cụ điều khiển browser" vì không tìm thấy `chromium-cli` (công cụ mặc định của `run` skill). Thực ra máy có SẴN Microsoft Edge (`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`) — chỉ cần `npm install playwright-core` (KHÔNG cần tải Chromium riêng, không cần thêm vào `package.json` dự án — cài tạm ở scratchpad) rồi `chromium.launch({executablePath: <đường dẫn Edge>, headless:true})` là điều khiển được. Lần sau cần verify UI/chụp màn hình, THỬ CÁCH NÀY trước khi kết luận "không có công cụ".

## 10. Important backend constraints (không thay đổi được từ phía FE)

- `accessToken` 8 giờ, `refreshToken` 7 ngày, KHÔNG rotate refresh token.
- `PUT /documents/:id` CHỈ nhận `{title, meta}` — mọi field khác bị 400.
- `Document` xoá hàng loạt theo tháng là soft-delete (không phải xoá vĩnh viễn).
- `MAX_IMPORT_ROWS`/`MAX_SYNC_ROWS` = 5000 dòng/file Excel.

## 11. Task log

- `docs/frontend/tasks/FE-00.md` — báo cáo đầy đủ Frontend Project Bootstrap (DONE 2026-09-05).
- `docs/frontend/tasks/FE-01.md` — báo cáo đầy đủ Authentication + App Shell + UI Foundation (DONE 2026-09-05).
- `docs/frontend/tasks/FE-02.md` — báo cáo đầy đủ RBAC / Permission UI Foundation (DONE 2026-09-05).
- `docs/frontend/tasks/FE-03.md` — báo cáo đầy đủ Users / Departments UI thật (DONE 2026-09-05).
- `docs/frontend/tasks/FE-04.md` — báo cáo đầy đủ Documents Core UI (DONE 2026-09-05).
- `docs/frontend/tasks/FE-05.md` — báo cáo đầy đủ FE Workflow UI (DONE 2026-09-06).
- `docs/frontend/tasks/FE-06.md` — báo cáo đầy đủ Assets Core UI (DONE increment đầu, 2026-09-06; 3 Remaining Issues fix ở DEV-035/036).
- `docs/frontend/tasks/FE-07.md` — báo cáo đầy đủ Medical Devices UI (DONE, 2026-09-07).
- `docs/frontend/tasks/FE-08.md` — báo cáo đầy đủ RBAC Admin UI (DONE, 2026-09-07).
- `docs/frontend/tasks/FE-09.md` — báo cáo đầy đủ Dashboard UI (DONE, 2026-09-07; nâng cấp UI + review browser thật + fix bug login 2026-09-09).
- `docs/frontend/tasks/FE-10.md` — báo cáo đầy đủ Asset Categories UI (DONE, 2026-09-09).
- `docs/frontend/tasks/FE-11.md` — báo cáo đầy đủ Audit Logs UI (DONE, 2026-09-09).
- `docs/frontend/tasks/FE-12.md` — báo cáo đầy đủ Notifications UI (DONE, 2026-09-10).
- `docs/frontend/tasks/FE-13.md` — báo cáo đầy đủ Notifications: Quản trị cho ADMIN (DONE, 2026-09-10; sửa thiếu mục sidebar theo phản hồi user, Mục 4b; chạy `seed-rbac.ts` theo yêu cầu user, Mục 4c).
- `docs/frontend/tasks/FE-14.md` — báo cáo đầy đủ Profile UI (DONE, 2026-09-10; kèm fix RBAC gap `USER_CHANGE_PASSWORD` cho 4 role).
- `docs/frontend/tasks/FE-15.md` — báo cáo đầy đủ Upload / Excel UI (DONE, 2026-09-10; kèm fix bug backend `fileUrl` chết + sửa 5 lỗi OpenAPI có sẵn + fix bug `getWorksheet(1)`/exceljs ở 3 hàm import/sync Excel — phát hiện khi user thử file thật, xem #27).
- `docs/frontend/phases/FE-01_MICRO_FIX_SESSION_RESTORE.md` — micro-fix Session Restore (PASS 2026-09-05).
- `docs/development/tasks/DEV-031.md` — fix regression route `documents/:id` (DONE 2026-09-06).
- `docs/development/tasks/DEV-032.md` — fix quy trình duyệt "3 cấp" (role duyệt sai) (DONE 2026-09-06).
- `docs/development/tasks/DEV-033.md` — fix dropdown "Khoa/Phòng" trống khi tạo đề xuất (DONE 2026-09-06).
- `docs/development/tasks/DEV-037.md` — fix lỗi E11000 (trùng username) trả 500 generic thay vì 409 rõ nghĩa, `updateMeService` + lưới đỡ chung `error.middleware.ts` (DONE 2026-09-10).
- `docs/development/tasks/DEV-038.md` — "Lịch sử duyệt" (Workflow History) + review kiến trúc Workflow + triển khai 2 đề xuất nâng cấp HIGH confidence (resubmit + ADMIN override) (DONE toàn bộ, 2026-09-10).
- `docs/development/tasks/DEV-039.md` — fix 403 "Xem chi tiết" từ tab "Lịch sử duyệt" (Policy ABAC mới + ngoại lệ reports) (DONE 2026-09-10).
- `docs/development/tasks/DEV-040.md` — fix IT không xem được tài liệu khoa khác dù đã gán permission (permission mới `DOCUMENT_VIEW_ALL_DEPARTMENTS`) (DONE 2026-09-10).
- `docs/development/tasks/DEV-041.md` — review RBAC toàn diện (role/permission chưa rõ ràng) + fix regression Policy bị mất + triển khai đủ 4 quyết định (khôi phục quyền IT, gộp department-scope, đổi chính sách nguồn-sự-thật seed-rbac.ts) (DONE 2026-09-11).
- `docs/development/tasks/DEV-042.md` — kiểm tra 4 điểm nghi vấn frontend (F1/F4/F5/F7: cache invalidation, RBAC guard duplicate, permission check bypass, StatusBadge domain mapping) (DONE 2026-09-12).
- `docs/development/tasks/DEV-043.md` — fix Dashboard "Tổng quan" IT toàn số 0 (không phải bug, mở nút "Xem theo khoa/phòng khác" cho IT) (DONE 2026-09-12).
- `docs/development/tasks/DEV-044.md` — thêm guard ADMIN-only cho bulk-delete-by-month (DONE 2026-09-12).
- `docs/development/tasks/DEV-045.md` — fix TOCTOU (RV05-07) dò trùng đề xuất sửa chữa Asset (DONE 2026-09-12).
- `docs/development/tasks/DEV-046.md` — bổ sung test cho `syncAssetOnDocumentApproved` (fix CRITICAL DEV-005, trước đó 0% coverage) (DONE 2026-09-12).
- `docs/development/tasks/DEV-047.md` — DEV-001A Phase B: gỡ lưới đỡ literal "ADMIN", chỉ dùng `isSystemRole` (đóng RV02-01) (DONE 2026-09-12).
- `docs/development/tasks/DEV-048.md` — dựng khung E2E test (supertest + mongodb-memory-server) + 10 test luồng CRITICAL (DONE 2026-09-12).
- `docs/frontend/tasks/FE-16.md` — Global UI Polish: audit toàn diện theo roadmap Mục 32 (0 vi phạm mới) + fix FE/BE contract drift `isSystemRole`/"ADMIN" sót lại từ DEV-047 ở 6 vị trí FE (DONE 2026-09-12).
- `docs/development/tasks/DEV-049.md` — tìm kiếm/lọc cho module File Upload, backend + frontend (DONE 2026-09-12).
- `docs/development/tasks/DEV-050.md` — Feature Roadmap A1: QR Code cho Asset — backend đã có sẵn, chỉ làm Frontend (DONE 2026-09-14).
- `docs/development/tasks/DEV-051.md` — Feature Roadmap A2: Upload chứng nhận kiểm định (Calibration Record) qua UI — vá bug backend đường dẫn chết `certificateFileUrl` (field mới `certificateFileId`) + endpoint tải file mới + UI upload/tải (tái dùng `FileUpload` component) (DONE 2026-09-15). Mục 6 (bổ sung cùng ngày, theo yêu cầu user): sửa lại chứng nhận đã lưu (upload nhầm file) — endpoint `PUT .../certificate` mới + `UpdateCalibrationCertificateModal` + tách `CertificateFileField` dùng chung.
- `docs/development/tasks/DEV-052.md` — Feature Roadmap A3: xác nhận Excel Import cho Asset — ĐÃ CÓ SẴN ĐẦY ĐỦ (backend + frontend), không cần code tính năng mới; bổ sung 10 unit test cho `assetExcel.service.ts` (gap phát hiện, user chọn làm) (DONE 2026-09-15).
- `docs/development/tasks/DEV-053.md` — Feature Roadmap A4: Lịch sử phiên bản tài liệu (Document Versioning) — model mới `DocumentVersion`, snapshot title/meta CŨ trong `updateDocumentService` (transaction), endpoint `GET /documents/:id/versions` (cùng ABAC với xem chi tiết), UI section "Lịch sử chỉnh sửa nội dung" + modal xem lại (tái dùng `DocumentMetaView`), CHỈ ĐỌC — không khôi phục (2 quyết định nghiệp vụ đã hỏi user trước khi code) (DONE 2026-09-15). Hoàn tất Nhóm A của roadmap.
- `docs/development/tasks/DEV-054.md` — Feature Roadmap B1: SLA & nhắc việc cho Workflow — `slaDays` mới trên `WorkflowTemplate.steps[]`, cron `workflowSlaAlerts.service.ts` (nhắc role bước đang chờ khi vượt SLA, escalate ADMIN khi vượt ≥2 lần SLA), endpoint chạy tay `POST /workflows/sla/run` + dashboard `GET /dashboard/workflow/overdue-approvals` + widget FE trong tab "Cảnh báo" (DONE 2026-09-15). **CẦN chạy `seed-rbac.ts`** để 2 role IT/PHONG_VAT_TU_TTB có permission mới trong DB thật — chưa tự chạy, cần user xác nhận.
- `docs/development/tasks/DEV-055.md` — Feature Roadmap B2: Lịch bảo trì chủ động — domain mới `AssetMaintenancePlan` (ĐỘC LẬP với luồng PROPOSE_REPAIR phản ứng), CRUD + `GET /assets/maintenance-plans/calendar`, permission mới `ASSET_MAINTENANCE_PLAN_VIEW/CREATE/UPDATE` (IT + PHONG_VAT_TU_TTB), UI: section trong `AssetDetailPage` + trang mới `/app/assets/maintenance-calendar` (lưới CSS Grid tự viết, không thêm thư viện) (DONE 2026-09-15). **ĐÃ chạy `seed-rbac.ts`** (user xác nhận) — vẫn cần gán tay qua UI "Phân quyền" cho IT/PHONG_VAT_TU_TTB.
- `docs/development/tasks/DEV-056.md` — Feature Roadmap B3: Quản lý vật tư tiêu hao (Inventory/Consumables) — module hoàn toàn mới, tách biệt `Asset`. 2 model mới `ConsumableItem` (tồn kho theo item+department) + `ConsumableTransaction` (giao dịch nhập/xuất BẤT BIẾN, `quantityOnHand` là running balance cập nhật atomic trong Mongo transaction). API `/api/inventory/*`, permission mới `CONSUMABLE_VIEW/CREATE/UPDATE/TRANSACTION_CREATE/ALERTS_TRIGGER` (IT+PHONG_VAT_TU_TTB đủ cả 5, USER chỉ VIEW). Cảnh báo tồn kho thấp: cron 08:15 hằng ngày (mirror `assetAlerts.service.ts`). UI: mục sidebar riêng "Vật tư tiêu hao" — `ConsumablesListPage` (`/app/inventory`) + `ConsumableDetailPage` (`/app/inventory/:id`, nhập/xuất kho + lịch sử giao dịch) (DONE 2026-09-15). **ĐÃ chạy `seed-rbac.ts`** (user xác nhận) — vẫn cần gán tay qua UI "Phân quyền" cho IT/PHONG_VAT_TU_TTB.
- `docs/development/tasks/DEV-057.md` — Feature Roadmap B4: Quản lý nhà cung cấp & hợp đồng bảo trì (Vendor & Contract Management) — module hoàn toàn mới, KHÔNG nhồi field vào `Asset`. 2 model mới `Vendor` + `Contract` (`assets: ObjectId[]` nhúng trực tiếp — 1 hợp đồng ↔ NHIỀU tài sản, KHÔNG collection trung gian riêng). API `/api/vendors/*` + `/api/contracts/*`, permission mới `VENDOR_VIEW/CREATE/UPDATE` + `CONTRACT_VIEW/CREATE/UPDATE/ALERTS_TRIGGER` (IT+PHONG_VAT_TU_TTB đủ cả 7, USER chỉ VIEW cả 2). Cảnh báo hợp đồng sắp hết hạn: cron 08:20 hằng ngày, CHỈ gửi role PHONG_VAT_TU_TTB (không broadcast theo phòng ban, khác B3). UI: 2 mục sidebar riêng "Nhà cung cấp" (`/app/vendors`) + "Hợp đồng bảo trì" (`/app/contracts`), component mới `AssetMultiPicker` (chọn nhiều asset, KHÔNG sửa `AssetPicker` có sẵn), section CHỈ ĐỌC "Hợp đồng bảo trì" mới trong `AssetDetailPage` (DONE 2026-09-16). **ĐÃ chạy `seed-rbac.ts`** (user xác nhận) — vẫn cần gán tay qua UI "Phân quyền" cho IT/PHONG_VAT_TU_TTB.

## 12. Liên kết toàn bộ tài liệu FE

`FE_CONTEXT.md` (entry point) · `FE_ARCHITECTURE.md` · `API_REFERENCE.md` · `AUTH_RBAC_MAP.md` · `DOCUMENT_DOMAIN_MAP.md` · `ROUTE_PERMISSION_MAP.md` · `STATE_MAPPING.md` · `DATA_FLOW.md` · `UI_REQUIREMENTS.md` · `ERROR_HANDLING.md` · `SHARED_COMPONENTS_LIBRARY.md` · `FE_KNOWLEDGE_BASE_AUDIT.md` · `FE_FOUNDATION_SPEC.md` (coding contract chính thức, đọc SAU khi đã đọc các file trên)
